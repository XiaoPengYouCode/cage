#set document(
  title: "椎间融合器骨改建拓扑优化的有限元建模与伴随灵敏度分析",
  author: "【待填：作者姓名】",
)
#set page(
  paper: "a4",
  margin: (left: 2.8cm, right: 2.6cm, top: 2.6cm, bottom: 2.4cm),
  numbering: "1",
)
#set text(lang: "zh", region: "cn", size: 10.5pt)
#set par(first-line-indent: 2em, leading: 0.72em, justify: true)
#set heading(numbering: "1.1")

#let todo(body) = block(
  fill: rgb("#fff8e5"),
  stroke: rgb("#d18b00"),
  radius: 3pt,
  inset: 8pt,
  width: 100%,
)[
  *【待填/待确认】* #body
]

#let flowbox(body, fill: rgb("#f8fbff"), stroke: rgb("#5d83a8")) = block(
  width: 100%,
  fill: fill,
  stroke: stroke,
  radius: 3pt,
  inset: 6pt,
)[#align(center)[#body]]

#let smallflowbox(body, fill: rgb("#f8fbff"), stroke: rgb("#5d83a8")) = block(
  width: 100%,
  fill: fill,
  stroke: stroke,
  radius: 3pt,
  inset: 4.5pt,
)[#align(center)[#text(size: 9pt)[#body]]]

#let rightarrow = text(size: 14pt, weight: "bold", fill: rgb("#4f5f6f"))[→]
#let downarrow = text(size: 13pt, weight: "bold", fill: rgb("#4f5f6f"))[↓]

#let figplan(id, title, body) = figure(
  block(
    width: 100%,
    height: 34mm,
    fill: rgb("#f6f7f8"),
    stroke: rgb("#9aa3ad"),
    radius: 3pt,
    inset: 8pt,
  )[
    *待实验图 #id：#title* \
    #body
  ],
  caption: [#title（待实验数据生成后替换）。],
)

#align(center)[
  #text(size: 18pt, weight: "bold")[椎间融合器骨改建拓扑优化的有限元建模与伴随灵敏度分析]

  #v(1.2em)
  #text(size: 14pt)[硕士学位论文草稿]

  #v(2em)
  #table(
    columns: (3cm, 8cm),
    align: (right, left),
    [作者：], [【待填：作者姓名】],
    [学号：], [【待填：学号】],
    [专业：], [【待填：专业名称】],
    [导师：], [【待填：导师姓名】],
    [学院：], [【待填：学院名称】],
    [日期：], [【待填：提交日期】],
  )
]

#pagebreak()

= 摘要

椎间融合器的拓扑结构需要同时满足早期力学支撑和术后骨长入需求。结构刚度过高会削弱周围骨组织的力学刺激，孔隙或材料分布过弱又会增加沉降和局部失稳风险。因此，本文将融合器设计问题建模为力学场、骨密度演化和设计变量共同耦合的约束优化问题。

本文围绕椎间融合器-骨组织耦合系统建立有限元拓扑优化模型。首先，将融合器设计区域、皮质骨区域、松质骨区域和目标骨长入区域统一离散为六面体单元集合，并采用小变形线弹性假设建立多材料平衡方程。其次，以单元应变能相关量构造力学刺激因子 `SF`，通过分段骨改建函数描述目标骨区密度随时间步的增长、静止和吸收行为。再次，将多时间步骨密度递推写成离散动力系统，并以终态目标骨量最大化为目标，推导伴随方程、伴随载荷、骨密度伴随变量和设计变量梯度。最后，采用移动渐近线法在体积分数约束下更新融合器材料分布，形成“正向预测-反向求导-设计更新”的闭环。

本文当前阶段的重点是完成科研问题定义、数学模型、离散推导、算法流程和实验方案。大规模数值实验尚未完成，因此涉及最终性能、收敛曲线、云图和对照误差的内容均以【待填/待确认】标记。开源计算实现被定位为模型复现和实验审计手段，服务于有限元方程、骨改建递推和伴随灵敏度计算，不构成论文的中心贡献。当前论文稿已给出算法总流程、正向/反向时间步关系、一次优化迭代数据流和验证层级等流程图，为后续补充真实实验结果提供可直接延展的论文框架。

关键词：椎间融合器；骨改建；有限元；拓扑优化；伴随法；移动渐近线法

#pagebreak()

= Abstract

The topology of an interbody fusion cage should provide early mechanical support while maintaining a mechanical environment favorable for postoperative bone ingrowth. Excessive stiffness may reduce mechanical stimulus in the surrounding bone, whereas insufficient material support may increase subsidence or local instability. The design task is therefore formulated as a constrained optimization problem coupling mechanical equilibrium, bone-density evolution, and spatial design variables.

This thesis develops a finite-element topology optimization model for the coupled cage-bone system. The cage design region, cortical bone, trabecular bone, and target ingrowth region are discretized by hexahedral elements under a small-strain linear-elastic assumption. A mechanical stimulus factor is constructed from element strain-energy terms, and a piecewise bone-remodeling law describes density formation, lazy-zone behavior, and resorption in the objective bone region. The multi-step density update is written as a discrete dynamical system. With terminal bone mass as the objective, the adjoint equation, adjoint load, density adjoint recursion, and design gradient are derived. The method of moving asymptotes is then used to update the cage material distribution under a volume constraint, forming a closed loop of forward prediction, backward sensitivity analysis, and design update.

The current manuscript focuses on problem formulation, mathematical modeling, discrete derivation, algorithmic procedure, and experimental design. Large-scale numerical experiments have not yet been completed; all final numerical values, contour plots, convergence curves, comparison errors, and result-dependent conclusions are explicitly marked as pending. The open computational implementation is treated as a reproducibility and audit mechanism for the mathematical model rather than the central scientific contribution.

Keywords: interbody fusion cage; bone remodeling; finite element method; topology optimization; adjoint method; method of moving asymptotes

#pagebreak()
#outline(title: "目录")
#pagebreak()

= 第 1 章 绪论

== 1.1 研究背景

腰椎退行性疾病、椎间盘高度丢失和节段不稳等问题常通过椎间融合手术恢复稳定性。椎间融合器作为植入物，一方面承担术后早期的轴向支撑和抗弯抗扭作用，另一方面为骨组织长入提供空间。若融合器整体刚度过高，载荷更多通过植入物传递，周围骨组织受到的有效刺激降低，可能出现应力遮挡；若融合器结构过于稀疏或局部刚度不足，又可能造成沉降、端板破坏或界面微动增大。融合器设计需要在力学安全性、骨长入潜力和材料用量之间取得平衡。

有限元方法可以描述复杂骨-植入物结构在外载荷下的位移、应力和应变能分布。拓扑优化进一步将设计变量引入有限元模型，使材料分布能够根据目标函数和约束自动调整。对于融合器骨长入问题，本文采用骨组织力学刺激响应模型，预测不同设计在若干时间步之后产生的目标骨量，并据此反向更新融合器结构。

本文研究对象可以概括为：给定骨区几何、融合器候选设计区域、材料参数和多组载荷，寻找一组融合器单元材料密度，使术后目标骨区的终态骨量尽可能高，同时控制材料用量并保持有限元问题可求解。该问题包含三个紧密连接的层次：力学平衡方程给出位移场，骨改建方程给出密度随时间的演化，优化算法给出材料分布的更新方向。

这一研究对象具有明显的空间非均匀性。融合器不同位置对骨长入的贡献并不相同：靠近界面和载荷传递路径的位置，材料改变可能显著改变周围骨单元的应变能；远离主要传力区域的位置，即使增加材料，也可能只提高局部刚度，对目标骨量贡献有限。传统参数化设计通常只能调整孔隙率、孔径、支柱直径或少量几何参数，难以表达这种单元级差异。拓扑优化把材料密度定义到设计单元上，使模型可以在空间上细致地区分“应保留材料的位置”和“可释放材料的位置”。

从生物力学角度看，骨长入是随时间逐步累积的过程。某一时间步的材料分布影响当前位移场，当前位移场影响力学刺激，力学刺激影响下一步骨密度，下一步骨密度又会改变结构刚度并影响后续位移。这种反馈使问题天然具有动态系统特征。本文将骨密度演化写成离散时间递推，目的就是保留这种“结构-力学-生物响应”的闭合关系。

本文不把融合器结构简单评价为“刚”或“软”。理想结构应在承载路径上提供足够支撑，同时让目标骨区获得适度力学刺激。过低刺激可能导致骨吸收或生长不足，过高刺激也可能进入不利区间。本文使用分段骨改建函数表达这种非单调关系，使优化目标从单纯增加刚度转向塑造合适的刺激分布。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr, 0.42cm, 1fr, 0.42cm, 1fr),
      stroke: none,
      smallflowbox([临床需求#linebreak()力学支撑#linebreak()骨长入空间], fill: rgb("#eef6ff")),
      align(center)[#rightarrow],
      smallflowbox([数学建模#linebreak()多材料有限元#linebreak()骨改建递推], fill: rgb("#f5fbf1")),
      align(center)[#rightarrow],
      smallflowbox([优化目标#linebreak()终态骨量最大#linebreak()材料用量受限], fill: rgb("#fff6e8")),
      table.cell(colspan: 5)[#v(0.4em)],
      table.cell(colspan: 5)[#align(center)[#downarrow]],
      table.cell(colspan: 5)[#flowbox([正向预测位移与骨密度  →  反向求解伴随变量  →  更新融合器材料分布], fill: rgb("#f8fafc"))],
    )
  ],
  caption: [椎间融合器骨改建拓扑优化问题的研究主线。],
)

== 1.2 研究意义

本文的意义首先在于将融合器结构设计从一次性力学评价推进到可微分的动态骨改建优化。有限元位移场只反映当前设计在当前载荷下的响应，骨密度递推进一步给出未来若干时间步内目标骨区的变化。把终态骨量纳入目标函数后，优化算法可以识别哪些融合器单元对后续骨形成贡献更大，哪些位置增加材料收益较低，从而形成更贴近生物力学结果的设计更新方向。

其次，伴随灵敏度分析使大规模设计变量问题具有可计算性。若对每个设计单元逐一扰动并重复完整正向仿真，计算量会随设计变量数量线性增长，完整模型难以承受。伴随法通过一次反向递推和有限次伴随求解，把终态骨量对所有设计变量的梯度统一计算出来，是本文优化闭环能够成立的关键。

再次，本文强调实验证据链的可审计性。骨改建拓扑优化的可信度取决于输入区域、材料映射、载荷、正向位移、骨密度更新、伴随载荷、伴随位移、梯度和设计更新之间是否闭合。论文后续实验会保存每一轮迭代的关键中间量，使结果可以从终态结构追溯到具体方程和具体时间步。

从方法研究角度看，本文的价值在于把骨改建问题明确写成 PDE 约束优化与离散动力系统耦合问题。有限元平衡方程处理空间力学场，骨密度递推处理时间演化，移动渐近线法处理带约束的高维设计更新。三者组合后，研究者可以分别检查模型假设、导数公式和优化行为。若最终结构表现不理想，也可以判断问题来自刺激函数、载荷设置、约束形式、线性求解误差或优化器参数，避免只看到一个难以解释的终态模型。

从工程应用角度看，本文的意义还在于提供一套可复核的设计生成逻辑。植入物结构优化必须面对安全性和可解释性要求。一个优化结构如果只有最终几何，没有目标函数、约束、载荷、材料参数和中间状态记录，后续很难进入严肃评估。本文把每一步计算证据纳入论文框架，后续实验可以直接围绕这些证据组织结果章节。

== 1.3 国内外研究现状

椎间融合器相关研究已经从“材料和外形是否足够承载”逐步发展到“结构如何同时服务于力学稳定与骨长入”的层次。与本文直接相关的研究现状可以归纳为四条主线：椎间融合器与多孔植入物设计、骨改建力学刺激模型、伴随法与拓扑优化、移动渐近线法与约束优化。

=== 1.3.1 椎间融合器与多孔植入物

椎间融合器设计最早更关注材料选择、节段稳定性和沉降风险，随着增材制造与多孔金属加工的发展，研究重点逐步转向孔隙结构、应力遮挡和骨长入环境之间的平衡。Kang 等在可降解腰椎融合器研究中，已经把多孔 cage 设计明确表述为强度、连通孔隙和应力传递之间的折中问题，并采用全局-局部 topology optimization 生成多孔结构 [5]。这类工作说明，融合器设计已经不再局限于少量经验几何参数，而是开始使用单元级材料分布来塑造整体力学与孔隙形态。

随后，面向多孔或晶格 cage 的有限元与实验研究进一步增加。Ho 等基于 3D 打印可降解融合器的动物实验表明，topology optimization 生成的结构不只是数值上可行，而且已经进入生物力学和骨桥接效果的实验评价阶段 [6]。针对金属晶格 cage 的研究也显示，晶格拓扑、材料体系和载荷工况之间的耦合会显著影响融合器刚度、界面应力和目标骨长入环境 [7]。这些研究共同说明，多孔/晶格融合器设计已经从“提高孔隙率”推进到“针对特定生物力学目标的结构优化”。

=== 1.3.2 骨改建力学刺激模型

骨改建模型的理论基础来自骨组织对机械使用的长期适应。Huiskes 等的经典工作指出，骨小梁形态的维持与重构受到局部机械刺激驱动，骨组织既不会对所有刺激单调增密，也不会在所有情况下保持不变 [2]。这一观点为后续将应变能、应变或应力相关量构造成刺激指标提供了直接依据。

在机制层面，Robling 和 Turner 对骨建模与骨改建的机械信号通路进行了综述，强调骨细胞感知、惰性区、成骨和吸收响应之间存在非线性分段关系 [8]。这类生物学背景使“低刺激吸收、适度刺激成骨、过高刺激不利”的分段函数形式具有合理性。进一步地，Martínez-Reina 等把有限元力学响应与骨细胞活动显式耦合，说明 FE + 机械刺激模型已经能够被写成可离散、可迭代的骨改建计算框架 [9]。Giorgio 等则从扩散与连续介质角度发展 mechanically driven remodeling 模型，说明骨改建刺激模型本身仍在持续扩展 [10]。与这些工作相比，本文选择单元离散和时间步递推的形式，重点是把骨改建预测与融合器拓扑优化变量直接耦合。

=== 1.3.3 伴随法与拓扑优化

连续体拓扑优化的经典主线可以追溯到 Bendsøe 和 Kikuchi 的均匀化方法工作，它奠定了“材料在设计域内重新分配”这一基本思想 [11]。随后，Bendsøe 和 Sigmund 对材料插值和密度法思路进行了系统发展，使相对密度设计变量与有限元刚度矩阵之间的关系能够被稳定表达 [12]。这条路线使大规模设计变量优化具备了统一的数学框架。

伴随思想之所以重要，是因为拓扑优化往往包含成千上万甚至更多设计变量。若对每个设计变量做有限差分扰动，计算量会难以承受，因此梯度/灵敏度分析成为主流路径。Tang 等对静力学结构拓扑优化的综述表明，当前主流方法仍以灵敏度驱动的密度法和约束优化为核心 [13]。Kazakis 等进一步指出，拓扑优化的研究重点已经延伸到结果解释、计算代价和 3D 打印制造衔接 [14]。与传统静态柔度优化不同，本文处理的是“终态骨量”这种随时间步递推的目标，因此后续伴随推导不仅涉及有限元状态方程，还涉及骨密度递推链上的反向传播。

=== 1.3.4 移动渐近线法与约束优化

在带体积分数约束的大规模结构优化中，移动渐近线法是使用最广的优化器之一。Svanberg 提出的 MMA 通过构造局部凸近似和可移动渐近线，使高维约束优化能够稳定更新 [4]。在拓扑优化领域，MMA 与密度法的结合已经成为标准配置，相关理论与应用可在 Bendsøe 和 Sigmund 的专著中看到系统总结 [3]。

2000 年后的综述同样表明，连续体拓扑优化在工程实践中通常采用灵敏度分析加约束优化的组合路线，而 MMA 仍是其中的主力方法 [15]。对本文而言，MMA 的作用不是提供新的生物学模型，而是把三工况聚合得到的目标梯度和体积约束转化为可执行的设计更新。因此，本文的学术重点仍然在于“骨改建目标如何建模”和“多时间步伴随灵敏度如何推导”，而 MMA 则承担稳定实现约束更新的算法角色。

综合以上研究现状可以看到，现有工作已经分别回答了三类问题：多孔融合器如何从经验设计走向结构优化，骨改建如何由机械刺激驱动并写成有限元可计算模型，大规模结构优化如何依靠伴随灵敏度和 MMA 完成高维约束更新。本文的工作正是把这三条主线汇合起来：在完整参考模型上，以终态目标骨量为优化目标，把有限元平衡、骨密度递推、伴随反向求导和材料分布更新组织成一条可审计的闭环链路。

== 1.4 研究内容与技术路线

本文研究内容包括六个方面。

第一，建立融合器-骨组织耦合有限元模型。将皮质骨、松质骨、融合器非设计区域、融合器设计区域和目标骨长入区域划分为不同单元集合，定义各自的材料插值规律和边界条件。

第二，建立骨密度演化模型。根据单元位移计算目标骨区的应变能相关量，再构造力学刺激因子 `SF`，通过分段函数得到骨密度增量，并对密度上下界进行截断。

第三，推导多时间步伴随灵敏度。以终态骨量为目标，从最后一个时间步开始反向递推，推导伴随载荷、伴随位移、骨密度伴随变量和设计变量梯度。

第四，建立三工况聚合优化。分别计算三个载荷工况下的终态骨量和梯度，将其聚合为总体目标函数与总体梯度，并在体积约束下用移动渐近线法更新设计变量。

第五，将优化得到的伪密度场转换为可制造的 Voronoi 骨架几何，并补入一层“目标等效刚度到几何参数”的标定逻辑。具体包括密度加权种子采样、Lloyd 型 CVT 松弛、受限 Voronoi 单元构造、脊线提取、骨架体素化，以及伪密度目标模量到杆径、seed 密度或单元尺度的均匀化映射。

第六，设计实验验证方案。当前尚未完成大规模实验，本文先给出输入完整性验证、小模型闭环验证、完整模型求解验证、优化收敛验证、标定层验证、骨架回代有限元验证和可选历史对照验证的实验设计。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr),
      stroke: none,
      flowbox([1. 区域与材料定义：骨区、融合器、设计单元、目标骨区], fill: rgb("#eef6ff")),
      align(center)[#downarrow],
      flowbox([2. 有限元正向求解：给定 `x` 与 `b_t`，求解 `K(x,b_t) u_t = f`], fill: rgb("#f5fbf1")),
      align(center)[#downarrow],
      flowbox([3. 骨改建递推：由 `SF_t` 计算 `b_(t+1)`，得到终态骨量], fill: rgb("#fff6e8")),
      align(center)[#downarrow],
      flowbox([4. 伴随反向递推：由终态目标反推每个时间步的灵敏度], fill: rgb("#f7f2ff")),
      align(center)[#downarrow],
      flowbox([5. 约束优化更新：聚合三工况梯度，用 MMA 更新融合器材料分布], fill: rgb("#edf9f5")),
    )
  ],
  caption: [本文技术路线图。],
)

== 1.5 论文结构安排

本文共分七章。第 1 章介绍研究背景、意义、研究现状和技术路线。第 2 章建立有限元、骨改建、目标函数和优化问题的数学定义。第 3 章推导多时间步伴随方程和设计梯度。第 4 章说明算法闭环与计算实现边界。第 5 章在三工况优化与验证框架之后，补充从伪密度场到 Voronoi 骨架几何的制造化流程，并说明必须补入的等效刚度标定层。第 6 章设计实验方案与待填结果表，其中包含标定层验证与骨架回代有限元验证。第 7 章总结当前工作并提出后续实验计划。

= 第 2 章 数学模型与问题定义

== 2.1 计算域与设计变量

设完整离散域为 `Omega`，其中融合器设计区域为 `Omega_d`，目标骨长入区域为 `Omega_o`，其余骨组织和非设计植入物区域作为背景区域参与受力。设计区域被离散为 `n_d` 个六面体单元，设计变量记为

$
x = (x_1, x_2, ..., x_(n_d))^T, quad 0 < x_(min) <= x_i <= 1.
$

其中 `x_i` 表示第 `i` 个设计单元的相对材料密度。`x_i = 1` 表示该处接近实体材料，`x_i` 越小表示该处材料占比越低。目标骨区被离散为 `n_o` 个单元，第 `j` 个目标骨单元在时间步 `t` 的骨密度记为 `b_j^t`，整体向量记为 `b^t`。

三类核心未知量分别为：设计变量 `x`、时间相关骨密度 `b^t` 和有限元位移 `u^t`。设计变量在一次正向骨改建预测中保持不变，骨密度和材料刚度随时间步更新，位移由当前刚度矩阵和外载荷共同决定。

设计区域和目标骨区的区分是本文建模的关键。设计区域代表融合器内部可调材料分布，目标骨区代表希望促进骨形成或骨保持的周围骨组织。两者在空间上可以相邻，也可以通过界面和载荷路径间接耦合。设计变量不直接等于目标骨密度；它先改变融合器刚度，再改变骨区位移和能量，最后通过骨改建方程影响目标骨密度。这一层间接关系使灵敏度推导更加复杂，也使伴随法具有必要性。

本文把设计变量限定在有界区间内。下界 `x_(min)` 用来避免刚度矩阵因局部空洞过多而病态，上界 1 对应实体材料。目标骨密度也限定在 `[b_(min), b_(max)]` 内。下界用于避免刺激因子中出现除以零，上界用于表达骨密度的生理饱和值。上下界不仅是数值保护，也带有物理含义：材料和骨组织都不能无限削弱或无限增长。

从网格角度看，六面体单元为本文提供了统一的数据结构。每个单元包含 8 个节点、每个节点 3 个平移自由度，因此单元位移向量为 24 维。设计单元、目标骨单元和背景单元都可以共享同一个节点坐标和连接关系，只在材料映射和优化角色上不同。这种设置使有限元装配、能量计算和伴随梯度累积可以使用同一套单元核。

== 2.2 有限元平衡方程

本文采用小变形线弹性假设。第 `e` 个六面体单元的节点位移向量为 `u_e in R^24`，应变位移矩阵为 `B_e`，无量纲本构矩阵为 `D_0`。若该单元等效弹性模量为 `E_e`、体积为 `V_e`，则单元刚度矩阵写为

$
K_e(E_e) = E_e V_e B_e^T D_0 B_e.
$

全局装配后，给定载荷工况 `c` 和时间步 `t`，位移场满足

$
K^c(x, b^t) u_(c,t) = f^c.
$

其中 `K^c(x,b^t)` 由设计区域材料模量、目标骨区材料模量和背景区域材料模量共同装配得到；`f^c` 为第 `c` 个载荷工况下的外载荷向量。底面节点施加固定约束，顶面区域通过参考点控制表达平移和小转角，使外力和力矩能够一致地传递到端面节点。

有限元方程也可以从虚功原理得到。对任意满足齐次位移约束的虚位移 `v`，线弹性弱式为

$
sum_(e in Omega) integral_(Omega_e) epsilon(v_e)^T D_e epsilon(u_e) dif Omega
  = v^T f.
$

若在单元内采用与当前六面体离散一致的应变位移矩阵，则 `epsilon(u_e) = B_e u_e`。将单元积分离散后，可得到前文给出的单元刚度矩阵。全局刚度矩阵由所有单元刚度按节点自由度装配得到。不同材料区域的差异全部体现在 `E_e` 或 `D_e` 上，因此材料插值和骨密度更新可以自然进入刚度矩阵。

边界条件的处理直接影响位移场可靠性。底面固定用于消除刚体平移和刚体转动，并模拟下端约束。顶面参考点耦合用于把集中力或力矩分配到端面节点。若简单把载荷施加在单个节点上，局部应力和位移会受到网格偶然性的影响；参考点耦合可以让端面以近似刚体方式受载，使三工况之间具有更一致的力学含义。

在伴随推导中，正向刚度矩阵还会作为伴随方程的系数矩阵重复出现。线弹性小变形假设带来两个便利：一是刚度矩阵在当前材料状态下对位移线性，二是矩阵在常规材料本构下对称。这样正向问题和伴随问题可以共享同一类线性系统结构。若后续引入接触、几何非线性或非线性材料，伴随方程需要重新从非线性残差的雅可比矩阵推导。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr, 0.38cm, 1fr, 0.38cm, 1fr),
      stroke: none,
      smallflowbox([底面约束#linebreak()`u = 0`], fill: rgb("#eef6ff")),
      align(center)[#rightarrow],
      smallflowbox([多材料刚度#linebreak()`K(x,b_t)`], fill: rgb("#f5fbf1")),
      align(center)[#rightarrow],
      smallflowbox([顶面参考点#linebreak()力/力矩载荷], fill: rgb("#fff6e8")),
      table.cell(colspan: 5)[#v(0.4em)],
      table.cell(colspan: 5)[#align(center)[#downarrow]],
      table.cell(colspan: 5)[#flowbox([求解节点位移 `u_(c,t)`，再切片得到每个单元的 24 自由度位移 `u_e`], fill: rgb("#f8fafc"))],
    )
  ],
  caption: [有限元正向问题的输入输出关系。],
)

== 2.3 材料插值模型

设计区域采用三次密度插值。令融合器实体模量为 `E_c0`，最小模量为 `E_cmin`，则设计单元 `i` 的等效模量为

$
E_c(x_i) = min(E_(c,min) + E_(c,0) x_i^3, E_(c,0)).
$

目标骨区的等效模量由骨密度决定。令骨组织上限密度为 `b_max`，实体骨模量为 `E_b0`，最小骨模量为 `E_bmin`，则

$
E_b(b_j^t) = min(E_(b,min) + E_(b,0) (b_j^t / b_(max))^3, E_(b,0)).
$

三次插值使低密度区域刚度贡献较弱，高密度区域逐渐接近实体材料。该形式同时提供明确导数：

$
(partial E_c) / (partial x_i) = 3 E_(c,0) x_i^2, quad
(partial E_b) / (partial b_j) = 3 E_(b,0) (b_j)^2 / b_(max)^3.
$

这两个导数将在伴随梯度推导中直接出现。

三次插值的作用可以从优化行为理解。若采用线性插值，中间密度单元会同时具有较高刚度和较低材料用量，优化结果容易保留大量灰度区域。三次插值削弱中间密度的刚度收益，使优化更倾向于把材料集中到真正有效的位置。对于融合器设计，这种倾向有助于形成更清晰的承载路径和孔隙空间。当前论文仍将最终制造可行性列为后续工作，因为仅靠三次插值不能保证最小杆径、最小孔径和连通性。

目标骨区的材料插值与设计区域不同。骨密度 `b_j^t` 是状态变量，由骨改建方程更新；设计变量 `x_i` 是控制变量，由优化器更新。二者都影响刚度矩阵，但物理含义不同。设计变量代表人工材料分布，骨密度代表组织状态。伴随推导中需要分别处理 `partial K / partial x` 和 `partial K / partial b`：前者产生设计梯度，后者产生骨密度伴随递推中的刚度反馈项。

模量下限 `E_(c,min)` 和 `E_(b,min)` 还承担数值稳定作用。若低密度单元模量为零，局部自由度可能失去约束并导致全局刚度矩阵奇异。保留小正数模量可以让“接近空洞”的区域仍参与装配，从而维持线性系统可解。实验报告中需要记录下限值，因为它会影响低密度区域的残余传力能力。

== 2.4 力学刺激因子 `SF`

给定目标骨单元 `j` 在时间步 `t` 的位移向量 `u_j^t`，定义单元能量核

$
H_j = V_j B_j^T D_0 B_j.
$

单元二次能量项为

$
q_j^t = 1 / 2 (u_j^t)^T H_j u_j^t.
$

本文用力学刺激因子 `SF_j^t` 表示该单元单位骨密度承受的等效能量刺激，公式中记为 `s_j^t`：

$
s_j^t = (q_j^t E_b(b_j^t)) / (V_j b_j^t).
$

该定义体现三层关系：位移场越大，二次能量项越大；骨密度通过等效模量影响载荷传递；在相同能量下，较低骨密度对应更高单位密度刺激。后续骨改建函数以 `SF_j^t` 作为输入。

`SF` 的形式来自对局部能量与骨密度关系的归一化。单元能量 `q_j^t` 反映当前载荷下该单元参与受力的程度，乘以骨模量项后可以表达密度对载荷承受能力的影响，再除以单元体积和当前骨密度，使刺激量更接近单位组织量承受的力学作用。这样处理后，较大单元不会仅因体积更大而获得更高刺激，较高密度单元也不会在同等能量下被简单判定为更高刺激。

该刺激定义并不等同于唯一的骨改建驱动指标。文献中常见的刺激量还包括应变、应变能密度、应力、流体剪切等。本文选择能量相关指标，是因为它可以直接由有限元单元位移和单元刚度核计算，并且导数形式清晰。对伴随推导而言，`SF` 对位移的导数只需要 `H_j u_j^t`，对骨密度的导数可以通过材料插值导数和除法项展开，适合大规模离散模型。

后续真实实验需要检查 `SF` 的数值分布。若大多数目标骨单元长期落在惰性区，优化器收到的骨改建梯度会偏弱；若大量单元落在过高刺激吸收区，增加局部刚度可能反而降低终态骨量。因此，结果章节应同时给出终态骨量曲线和 `SF` 区间统计，说明优化过程到底是在增加成骨区比例，还是在减少过高刺激或低刺激区域。

== 2.5 骨密度演化方程

目标骨区密度按离散时间步更新。令 `R(SF)` 为骨改建速率函数，`Delta t` 为时间步长，则

$
b_j^(t+1) = Pi_[b_(min), b_(max)](b_j^t + Delta t R(s_j^t)).
$

其中 `Pi_[b_min,b_max]` 表示截断到物理密度区间。骨改建速率函数采用分段形式，包含低刺激吸收区、惰性区、成骨区、过高刺激吸收区和高刺激平台区。当前实现对应的参数关系为

$
theta_1 = k(1 - s_s), quad theta_2 = k(1 + s_s).
$

#set par(first-line-indent: 0em)
#table(
  columns: (2.7cm, 4.2cm, 7.4cm),
  align: (center, center, left),
  [区间], [条件], [骨改建速率],
  [低刺激吸收], [`SF < theta_1`], [`R(SF) = c_1 (SF - theta_1)`],
  [惰性区], [`theta_1 <= SF <= theta_2`], [`R(SF) = 0`],
  [成骨区], [`theta_2 < SF <= theta_f`], [`R(SF) = c_2 (SF - (theta_2 + theta_f)/2)^2 + alpha r_sat`],
  [过高刺激吸收], [`theta_f < SF <= theta_m`], [`R(SF) = c_3 (SF - theta_m)^2 + r_over`],
  [平台区], [`SF > theta_m`], [`R(SF) = r_over`],
)
#set par(first-line-indent: 2em)

其中 `k`、`s_s`、`theta_f`、`theta_m`、`r_sat`、`alpha`、`r_over`、`c_1`、`c_2` 和 `c_3` 为模型参数。后续实验需要在参数表中给出最终取值、单位和来源。

分段函数的每一段都有明确生物力学解释。低刺激吸收区表示骨组织缺少足够机械刺激时产生密度下降；惰性区表示刺激处于维持范围内，骨密度近似保持不变；成骨区表示适度刺激促进骨形成；过高刺激吸收区表示过载可能导致不利响应；平台区表示极高刺激下速率进入饱和。该函数的非单调性使优化问题更接近真实骨适应过程，也使目标函数不再是简单的刚度单调函数。

分段函数在阈值处可能存在导数不连续。实际伴随计算需要使用分段导数 `R'(SF)`。在区间内部，导数可由对应线性或二次函数直接给出；在阈值点，单点不可导对连续变量影响较小，但数值实现仍应采用明确规则。本文后续验证会通过小模型检查 `R(SF)` 和 `R'(SF)` 的一致性，尤其关注阈值附近是否出现符号错误或突变导致的梯度异常。

时间步长 `Delta t` 控制单次骨密度更新幅度。时间步过大可能使密度在一次迭代中直接触碰上下界，削弱梯度连续性；时间步过小则需要更多正向和伴随求解才能体现长期变化。本文先把时间步数和时间步长作为给定参数，在实验中记录它们对收敛和终态骨量的影响。后续可以设计时间步消融实验，比较短期预测和长期预测对最终拓扑的差异。

== 2.6 优化目标与约束

设载荷工况集合为 `C = {1,2,3}`，每个工况下进行 `P` 个骨改建时间步。第 `c` 个工况的终态目标骨量定义为

$
B_c(x) = sum_(j=1)^(n_o) b_(c,j)^P.
$

本文将优化目标写为最大化三工况终态骨量之和：

$
max_x quad Phi(x) = sum_(c in C) B_c(x).
$

为了便于使用标准最小化优化器，也可等价写为

$
min_x quad J(x) = - sum_(c in C) B_c(x).
$

材料用量约束采用相对体积形式：

$
g(x) = sum_(i=1)^(n_d) x_i - sum_(i=1)^(n_d) x_i^0 <= 0.
$

其中 `x_i^0` 为初始设计密度。若需要写成物理体积形式，可在两项前同时乘以单元体积 `V_i`。设计变量还满足盒约束 `x_min <= x_i <= 1`。

采用终态骨量作为目标，意味着本文关注设计经过若干骨改建时间步后的结果，重点评价长期响应。这个目标具有延迟性：某个设计单元在初始时刻对位移影响不明显，却可能通过后续密度递推逐步改变目标骨区状态；另一个单元在初始时刻显著提高刚度，却可能把局部刺激推入不利区间。终态目标能够把这些时间累积效应纳入优化。

体积约束的作用是限定材料预算。若没有材料约束，优化器可能倾向于在多个传力路径上同时增加材料，使目标函数提升来自整体加固，削弱材料重分配问题本身的意义。以初始设计总量作为约束基准，可以让优化问题更像“在固定材料预算下重新分布材料”。这也便于后续比较初始结构与优化结构：材料总量相近，差异主要来自空间分布。

目标函数符号需要在论文和实现中保持一致。本文理论上使用 `Phi` 表示需要最大化的终态骨量和，使用 `J=-Phi` 表示最小化形式。实验表格应同时报告 `Phi` 和 `J` 的含义，避免把目标函数下降误读为骨量下降。若后续加入应力或孔隙约束，也应明确它们进入优化器时的符号约定。

= 第 3 章 伴随灵敏度推导

== 3.1 多时间步状态方程

对任一载荷工况 `c`，正向问题由两类状态方程构成。第一类是有限元平衡方程：

$
r_u^t(u^t, b^t, x) = K(x,b^t) u^t - f = 0.
$

第二类是骨密度递推方程：

$
r_b^t(b^(t+1), b^t, u^t, x) = b^(t+1) - G(b^t, u^t, x) = 0,
$

其中

$
G_j(b^t,u^t,x) = Pi_[b_(min),b_(max)](b_j^t + Delta t R(s_j^t)).
$

为了推导清晰，本章先忽略密度截断在边界处带来的不可导点；当密度位于上下界内部时，`Pi` 的导数为 1。当密度被截断时，实际实现可将对应导数置零或采用次梯度近似，后续实验需说明采用策略。

多时间步状态方程可以看成一个由 `P` 个正向单步映射串联而成的计算图。第 `t` 步的输入是 `b^t` 和固定设计 `x`，有限元方程先给出 `u^t`，骨改建映射再给出 `b^(t+1)`。终态目标 `Phi_c` 只直接依赖 `b^P`，但通过链式法则间接依赖所有中间状态。伴随法的本质，就是把这个计算图的梯度沿时间反向传播，同时把有限元平衡方程的位移导数通过线性伴随系统求解。

为避免符号混乱，本文把单工况推导写成省略工况上标的形式。实际三工况计算中，每个工况都有自己的 `u_c^t`、`b_c^t`、`psi_c^t` 和 `lambda_c^t`。设计变量 `x` 在三个工况之间共享，因此最终设计梯度需要对三个工况求和。这样的写法既保持公式简洁，也保留了多载荷优化的物理含义。

构造拉格朗日函数时，可以把所有时间步的有限元残差和骨密度残差加入目标函数：

$
cal(L) = Phi_c(b^P)
  + sum_(t=0)^(P-1) (lambda^t)^T (K^t u^t - f)
  + sum_(t=0)^(P-1) (psi^(t+1))^T (b^(t+1) - G(b^t,u^t,x)).
$

其中 `lambda^t` 是有限元平衡方程的乘子，`psi^(t+1)` 是骨密度递推方程的乘子。对 `u^t`、`b^t` 和 `x` 分别求变分，并选择合适的 `lambda^t` 与 `psi^t`，即可得到后续伴随方程和设计梯度。

== 3.2 目标函数微分

单工况终态目标函数取

$
Phi_c = sum_(j=1)^(n_o) b_j^P.
$

因此终端骨密度伴随变量为

$
psi_j^P = (partial Phi_c) / (partial b_j^P) = 1.
$

对时间步 `t` 的骨密度递推求微分，可得

$
d b^(t+1) = G_b^t d b^t + G_u^t d u^t + G_x^t d x.
$

其中 `G_b^t`、`G_u^t` 和 `G_x^t` 分别表示骨密度更新函数对当前骨密度、当前位移和设计变量的导数。位移微分由平衡方程给出：

$
K^t d u^t + (partial K^t / partial b^t d b^t) u^t
  + (partial K^t / partial x d x) u^t = 0.
$

若直接使用该式求 `d u^t / d x_i`，每个设计变量都需要一次线性化求解，计算量过高。伴随法的作用是把 `d u^t` 项通过一次伴随求解消去。

从微分结构看，`d b^(t+1)` 包含三类来源。第一类是密度自反馈：当前骨密度改变会改变当前模量，也会改变 `SF` 的分母，因此影响下一步密度。第二类是位移反馈：当前骨密度和设计改变刚度矩阵后，会改变位移场，位移变化又会改变单元能量和 `SF`。第三类是设计直接项：若某个模型把设计变量直接放入骨改建函数，则会产生 `G_x^t`；当前模型主要通过位移场传递设计影响，因此设计直接项较弱或为零。

有限元位移微分式中，`partial K / partial x` 和 `partial K / partial b` 都是三阶张量作用在方向微分上的简写。实际单元层面并不需要显式形成三阶张量。对设计单元 `i`，刚度变化可以写为

$
d K_i = H_i (partial E_c / partial x_i) d x_i.
$

对目标骨单元 `j`，刚度变化可以写为

$
d K_j = H_j (partial E_b / partial b_j) d b_j.
$

这种单元局部写法更适合实现，也更方便解释梯度项的来源。

== 3.3 伴随位移方程

设 `lambda^t` 为时间步 `t` 的伴随位移变量。为了消去目标函数微分中的 `d u^t`，令

$
K^t lambda^t = (G_u^t)^T psi^(t+1).
$

由于线弹性刚度矩阵在当前假设下为对称矩阵，伴随方程与正向方程使用同一个刚度矩阵，只是右端载荷不同。右端项

$
F_v^t = (G_u^t)^T psi^(t+1)
$

就是动态伴随载荷。它来自当前正向位移、当前骨密度和下一步骨密度伴随变量，并随时间步变化。

对第 `j` 个目标骨单元，若忽略密度截断，刺激因子对单元位移的导数为

$
(partial s_j^t) / (partial u_j^t)
  = (E_b(b_j^t)) / (V_j b_j^t) H_j u_j^t.
$

骨密度更新对位移的导数为

$
(partial G_j^t) / (partial u_j^t)
  = Delta t R'(s_j^t) (partial s_j^t) / (partial u_j^t).
$

因此单元层面的伴随载荷可写为

$
F_(v,j)^t
  = psi_j^(t+1) Delta t R'(s_j^t)
    (E_b(b_j^t)) / (V_j b_j^t) H_j u_j^t.
$

该式解释了伴随载荷的来源：若某个单元的终态目标对当前密度更敏感，且当前位移对力学刺激影响更大，则该单元在反向求解中会产生更大的等效载荷。

动态伴随载荷需要从单元载荷装配到全局节点自由度。第 `j` 个目标骨单元的 `F_(v,j)^t` 是 24 维向量，对应 8 个节点的三向自由度。若多个目标骨单元共享节点，它们的伴随载荷贡献需要在该节点自由度上相加。装配后的全局向量就是伴随线性系统右端项。这个装配过程与正向有限元装配类似，只是装配对象从刚度矩阵变成了载荷向量。

伴随载荷的符号取决于拉格朗日函数的约定。本文采用的约定使伴随方程写为 `K lambda = F_v`，并在骨密度伴随递推和设计梯度中显式保留刚度反馈项的符号。实验验证时，单纯检查伴随载荷是否非零还不够，必须用有限差分验证梯度方向。若设计变量沿负梯度方向微小变化后 `J` 没有下降，说明符号约定或梯度聚合存在问题。

伴随求解与正向求解共享当前时间步的材料状态。反向第 `t` 步不能使用终态骨密度装配刚度矩阵，也不能使用初始骨密度装配矩阵；它必须使用正向第 `t` 步真实使用过的 `b^t` 和设计 `x`。这也是保存正向时间步中间量的原因。若只保存终态结果，后续无法严格复现伴随方程。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr, 0.38cm, 1fr, 0.38cm, 1fr, 0.38cm, 1fr),
      stroke: none,
      smallflowbox([`b^0`], fill: rgb("#eef6ff")),
      align(center)[#rightarrow],
      smallflowbox([求 `u^0`#linebreak()更新 `b^1`], fill: rgb("#f5fbf1")),
      align(center)[#rightarrow],
      smallflowbox([···], fill: rgb("#f8fafc")),
      align(center)[#rightarrow],
      smallflowbox([求 `u^(P-1)`#linebreak()更新 `b^P`], fill: rgb("#fff6e8")),
      table.cell(colspan: 7)[#v(0.5em)],
      table.cell(colspan: 7)[#align(center)[#text(size: 9pt)[正向时间推进：由当前设计预测终态骨量]]],
      table.cell(colspan: 7)[#v(0.5em)],
      smallflowbox([`psi^0`], fill: rgb("#f7f2ff")),
      align(center)[#rightarrow],
      smallflowbox([由 `lambda^0`#linebreak()更新 `psi^0`], fill: rgb("#f7f2ff")),
      align(center)[#rightarrow],
      smallflowbox([···], fill: rgb("#f8fafc")),
      align(center)[#rightarrow],
      smallflowbox([`psi^P = 1`#linebreak()求 `lambda^(P-1)`], fill: rgb("#f7f2ff")),
      table.cell(colspan: 7)[#v(0.5em)],
      table.cell(colspan: 7)[#align(center)[#text(size: 9pt)[反向时间推进：由终态目标反推每一步灵敏度]]],
    )
  ],
  caption: [正向骨改建时间步与反向伴随时间步的关系。],
)

== 3.4 骨密度伴随变量递推

伴随位移求得后，骨密度伴随变量从 `t+1` 回传到 `t`。根据链式法则，密度伴随变量满足

$
psi^t = (G_b^t)^T psi^(t+1)
  - ((partial K^t) / (partial b^t) u^t)^T lambda^t.
$

第一项来自骨密度递推函数本身，表示当前骨密度通过骨改建公式影响下一步骨密度；第二项来自刚度矩阵对骨密度的依赖，表示当前骨密度改变会改变有限元位移，从而间接影响后续骨量。

对目标骨单元 `j`，刚度反馈项可写为

$
((partial K_j) / (partial b_j) u_j^t)^T lambda_j^t
  = (lambda_j^t)^T H_j u_j^t (partial E_b) / (partial b_j).
$

代入材料插值导数，得到

$
(partial E_b) / (partial b_j)
  = 3 E_(b,0) (b_j^t)^2 / b_(max)^3.
$

该项体现了骨密度对局部刚度的反馈作用。若某个目标骨单元的伴随位移与正向位移在能量核下相互作用强，则该单元密度变化对终态目标的影响更显著。

骨密度伴随变量 `psi^t` 可以理解为“时间步 `t` 的一单位骨密度变化对终态骨量的边际价值”。终态时 `psi^P = 1`，因为终态骨密度直接进入目标函数。向前一个时间步回传时，`psi^(P-1)` 同时包含两部分信息：一部分来自 `b^(P-1)` 通过骨改建函数直接影响 `b^P`，另一部分来自 `b^(P-1)` 改变刚度矩阵后影响 `u^(P-1)`，进而影响 `SF^(P-1)` 和 `b^P`。

该递推也解释了为什么骨密度状态必须保存在每个时间步。若只知道终态密度，无法判断早期某个单元的密度变化究竟是通过自反馈、刚度反馈还是刺激区间变化影响最终结果。伴随变量提供了一种可解释的诊断工具：`psi^t` 高的区域说明早期密度对终态目标敏感，`psi^t` 低的区域说明该处密度变化对最终骨量贡献较弱。

当骨密度处于截断边界时，`psi` 的传播需要谨慎解释。若密度已经达到上界，继续增加刺激可能无法提高密度，导数应减弱或为零；若密度达到下界，吸收项也可能被截断。后续实验应统计每个时间步有多少目标骨单元触碰上下界，避免把由截断造成的梯度消失误判为优化器失效。

== 3.5 设计变量梯度

设计变量 `x_i` 主要通过融合器设计单元刚度影响位移场。单工况目标对设计变量的梯度贡献为

$
(partial Phi_c) / (partial x_i)
  = - sum_(t=0)^(P-1)
    (lambda_i^t)^T H_i u_i^t (partial E_c) / (partial x_i)
  + (G_x^t)^T psi^(t+1).
$

在当前模型中，设计变量不直接进入目标骨区密度递推函数，只通过结构刚度影响位移场，因此主要项为刚度反馈项。采用三次材料插值后，

$
(partial E_c) / (partial x_i) = 3 E_(c,0) x_i^2.
$

三工况聚合后，最小化目标 `J = - sum_c Phi_c` 的梯度为

$
(partial J) / (partial x_i)
  = - sum_(c in C) (partial Phi_c) / (partial x_i).
$

该梯度进入移动渐近线法子问题，与体积约束梯度共同决定下一轮设计。

设计梯度的单元形式有明确的力学含义。项 `(lambda_i^t)^T H_i u_i^t` 是正向位移和伴随位移在单元能量核下的相互作用，描述设计单元刚度变化对终态目标的影响通道。若该项为正，增加该单元模量可能沿某个方向改变终态目标；若该项为负，减少材料可能更有利。乘上 `partial E_c / partial x_i` 后，梯度还会受到当前设计密度影响：密度越高，三次插值的导数越大，单位设计变化带来的刚度变化越明显。

在完整模型中，梯度向量长度等于设计单元数量。直接查看全部数值不直观，因此后续结果应给出梯度统计和空间分布。梯度统计包括最小值、最大值、均值、标准差和正负比例；空间分布可以用三维视图或切片展示。这样可以判断优化方向是否集中在界面附近、主要传力路径附近，或出现异常孤立高梯度点。

有限差分是验证设计梯度的直接方法。选取少量设计单元或随机方向 `p`，比较伴随方向导数 `nabla J(x)^T p` 与中心差分

$
(J(x + epsilon p) - J(x - epsilon p)) / (2 epsilon).
$

若二者在合理容差内一致，说明正向递推、伴随载荷、骨密度伴随和设计梯度符号基本可信。完整模型逐单元有限差分成本过高，但方向导数检查可以在小模型和缩减模型上完成。

== 3.6 推导中的近似与边界处理

本文当前推导包含三个需要在实验章节明确记录的近似。第一，骨密度截断在上下界处不可导，实际求导需要采用区间内导数、零导数或次梯度近似。第二，材料模量分桶用于数值装配时会引入离散化近似；理论推导按照连续插值给出，实验中需要说明分桶数量和误差影响。第三，参考点刚性耦合采用小转角线性关系，适用于本文的小变形线弹性假设。

这些近似不会改变算法主线，但会影响最终数值结果和误差解释。后续真实实验应在参数表、误差表和消融实验中逐项说明。

除上述三点外，本文还默认单步骨改建中的力学响应为准静态响应，即每个时间步先在当前材料状态下达到平衡，再更新骨密度。该假设忽略了真实组织改建中的连续时间过程和生物调控滞后，但它使模型能够在有限时间步内计算，并与拓扑优化框架结合。后续若引入连续时间模型，可以把骨密度方程写成常微分方程或偏微分方程，再用时间离散方法求解。

另一个近似是材料各向同性假设。皮质骨、松质骨和多孔融合器在真实结构中可能表现出各向异性，尤其是增材制造支柱结构和骨小梁方向性。本文先用等效各向同性模量描述区域差异，把研究重点放在骨改建目标和伴随灵敏度上。后续若有实验或文献参数支持，可以把 `D_0` 扩展为各向异性本构矩阵，并重新检查 `SF` 和梯度公式。

这些近似需要在结论中诚实呈现。它们并不削弱本文模型框架的价值，因为论文当前目标是建立可推导、可验证、可扩展的骨改建拓扑优化方法。真实数据补齐后，近似的影响可以通过参数敏感性分析、对照实验和消融实验逐步量化。

= 第 4 章 算法闭环与计算实现边界

== 4.1 算法总体闭环

完整算法由外层设计迭代和内层三工况骨改建预测组成。外层第 `k` 轮给定设计 `x^k`。对每个载荷工况，算法从初始目标骨密度出发，正向推进 `P` 个时间步，保存每一步位移、刺激因子、骨密度增量和骨量。随后从终态目标开始反向推进，构造动态伴随载荷，求解伴随位移，递推骨密度伴随变量，并累积设计梯度。三个工况完成后，将目标函数、约束和梯度聚合，交给 MMA 更新为 `x^(k+1)`。

该闭环的核心是把正向预测和反向解释绑定在同一组状态上。正向阶段回答“当前设计会产生怎样的骨密度演化”，反向阶段回答“终态骨量对当前设计的敏感路径在哪里”，优化阶段回答“在材料预算内下一轮应如何移动设计变量”。三者缺一项，算法都无法形成可解释的设计更新。

外层迭代的停止条件需要同时考虑设计变化和目标变化。当前基础判据采用 `delta = mean(abs(x^(k+1)-x^k))`，它衡量平均设计变量变化。当 `delta` 足够小，说明 MMA 已经难以在当前梯度和约束下继续移动设计。后续实验还应记录目标函数变化、约束违反量和梯度范数。如果设计变化很小但目标函数仍波动，可能说明线性求解误差或梯度噪声影响了优化稳定性。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr),
      stroke: none,
      flowbox([外层输入：当前融合器设计 `x^k`、初始骨密度 `b^0`、三组载荷], fill: rgb("#eef6ff")),
      align(center)[#downarrow],
      flowbox([对每个载荷工况：求解 `K(x^k,b^t)u^t=f`，计算 `SF^t`，更新 `b^(t+1)`], fill: rgb("#f5fbf1")),
      align(center)[#downarrow],
      flowbox([从 `psi^P=1` 反向：构造 `F_v^t`，求解 `K lambda^t=F_v^t`，更新 `psi^t`], fill: rgb("#f7f2ff")),
      align(center)[#downarrow],
      flowbox([聚合三工况：`J(x)`、`partial J/partial x`、`g(x)`、`partial g/partial x`], fill: rgb("#fff6e8")),
      align(center)[#downarrow],
      flowbox([MMA 子问题：在移动渐近线与盒约束内得到 `x^(k+1)`], fill: rgb("#edf9f5")),
    )
  ],
  caption: [骨改建拓扑优化算法闭环。],
)

== 4.2 正向求解步骤

正向求解步骤可概括为以下过程。

#set par(first-line-indent: 0em)
1. 读取当前设计变量 `x^k` 和当前骨密度 `b^t`。\
2. 由材料插值计算设计区域模量 `E_c(x_i)` 和目标骨区模量 `E_b(b_j^t)`。\
3. 装配多材料刚度矩阵 `K(x^k,b^t)` 并施加载荷、约束和参考点耦合。\
4. 求解位移 `u^t`。\
5. 对目标骨单元计算 `q_j^t`、`SF_j^t`、`R(SF_j^t)` 和 `b_j^(t+1)`。\
6. 保存该时间步的位移、刺激、密度、骨量和用于伴随求解的中间量。
#set par(first-line-indent: 2em)

正向求解的核心输出是时间序列 `{u^t, b^t, SF^t}`。这些量既用于结果解释，也用于反向伴随求导。

正向步骤中的材料更新需要严格按时间顺序执行。第 `t` 步有限元求解使用的是 `b^t`，骨改建更新后得到的 `b^(t+1)` 只能进入下一步。若在同一步中混用更新后的密度装配刚度矩阵，会改变离散时间模型的含义。论文后续实验记录应标明每个时间步输出的 `obj_bo_previous` 和 `obj_bo_next`，保证读者可以复原时间顺序。

正向求解还需要检查位移场的物理合理性。最大位移过大可能意味着材料下限过低、约束不足或载荷设置异常；最大位移接近零可能意味着载荷没有正确施加、约束过强或线性求解失败。单元能量和 `SF` 的分布也应与载荷方向和几何区域相符。正式实验中，至少应为每个工况报告最大位移、位移范数、能量范围和 `SF` 区间占比。

== 4.3 反向伴随步骤

反向伴随步骤从终态骨量开始。先令 `psi^P = 1`，然后对 `t = P-1, ..., 0` 依次执行：

#set par(first-line-indent: 0em)
1. 读取对应正向时间步的 `u^t`、`b^t` 和 `SF^t`。\
2. 根据 `R'(SF^t)`、`E_b(b^t)`、`H u^t` 和 `psi^(t+1)` 构造动态伴随载荷 `F_v^t`。\
3. 求解伴随线性系统 `K(x,b^t) lambda^t = F_v^t`。\
4. 根据刚度反馈项更新骨密度伴随变量 `psi^t`。\
5. 对设计单元累积梯度贡献。
#set par(first-line-indent: 2em)

伴随载荷必须由运行时正向结果生成。若伴随载荷固定为零，反向链路将无法表达终态骨量对设计变量的真实依赖。

反向步骤的时间顺序与正向步骤相反。第 `P-1` 步最先反向求解，因为它直接连接终态骨密度 `b^P`；第 0 步最后反向求解，因为它影响整条后续时间链。每一步伴随求解都需要读取同一时间步正向保存的位移和骨密度。若反向顺序或时间索引错位，伴随变量仍可能数值有限，但梯度会失去物理意义。

伴随求解的输出不仅是设计梯度的中间量，也可以作为解释工具。伴随位移较大的区域说明终态骨量对该处等效伴随载荷响应敏感；伴随载荷较大的区域说明骨改建函数在该处把位移变化强烈传递到终态目标。将正向位移、`SF`、伴随载荷和伴随位移放在一起分析，可以判断优化器为什么选择增加或减少某些设计单元。

== 4.4 移动渐近线法更新

移动渐近线法在每轮迭代中构造一个凸近似子问题。设当前设计为 `x^k`，下、上移动渐近线为 `L_i^k` 和 `U_i^k`。对目标函数梯度进行正负分解：

$
p_i^0 = max((partial J) / (partial x_i), 0), quad
q_i^0 = max(-(partial J) / (partial x_i), 0).
$

约束梯度也作同样分解。MMA 子问题使用形如

$
sum_i (p_i^0 / (U_i^k - x_i) + q_i^0 / (x_i - L_i^k))
$

的近似项控制变量更新。若连续两轮设计变化方向一致，渐近线适度放宽；若方向反复，渐近线收紧。该机制可以减少大规模非线性优化中的震荡。

本文当前约束为单一材料用量约束，后续可以在同一框架中加入应力、孔隙连通性、制造最小尺度等约束。每新增一个约束，都需要同步给出物理含义、数学表达式和梯度来源。

MMA 的优势在于每轮构造局部近似子问题，并通过移动渐近线控制更新幅度。移动渐近线相当于给每个设计变量设置一个局部信任区间，并根据历史变化自动调整。如果某个变量连续朝同一方向变化，说明当前局部近似较稳定，渐近线可以放宽；如果变量来回震荡，说明局部近似可能过激，渐近线会收紧。对于骨改建这种非线性、非单调目标，这种机制比简单梯度下降更稳健。

体积约束进入 MMA 时需要归一化处理。设计变量数量很大时，未归一化的目标梯度和约束梯度量级可能差异明显，影响子问题求解。实验中应记录目标值、约束值、目标梯度范数和约束梯度范数，必要时进行尺度调整。尺度调整不改变原始问题含义，但可以提高优化器的数值稳定性。

MMA 更新结果还需要经过盒约束检查。若大量设计变量长期停留在下界或上界，说明优化结构趋向清晰；若大量变量停留在中间密度，可能需要调整插值惩罚、移动限、滤波策略或制造约束。当前论文先保留基础 MMA 更新，后续可在实验中加入灰度比例统计，评价结果是否适合进一步几何重建或制造。

== 4.5 实现边界

论文的核心是有限元建模、骨改建递推、伴随灵敏度和约束优化。当前仓库中的开源计算实现用于复现实验、保存中间量和生成验证报告；其中使用 Python 生态完成数据组织、有限元求解接口和数值优化调度。该工具选择只服务于模型计算，不作为本文主要创新点。

商业有限元软件可作为后续误差对照来源，但主研究流程按照本文给出的离散方程和伴随推导组织。若后续引入不同求解器，论文需要比较的是有限元弱式、约束处理、线性求解容差和输出误差，而非工具名称本身。

实现边界还包括失败处理原则。科研计算中，静默生成结果比直接失败更危险。若线性系统没有收敛、伴随载荷为空、数组维度不一致、梯度出现非有限值或优化状态缺失，程序应停止并在验证报告中给出明确原因。论文中的“可复现”指结果可以被重新计算和审计，也指错误可以被定位，最终输出目录必须能支撑这些检查。

数据组织应服从数学结构。正向时间步、反向时间步、工况聚合和外层迭代分别对应不同层级的目录或记录。这样的组织方式能让读者从某个最终梯度值追溯到某个工况、某个时间步、某个伴随载荷和某个正向位移。若未来更换求解器或添加约束，只要仍遵守同一数学结构，论文方法部分就不需要大幅改写。

= 第 5 章 三工况优化、数据记录与验证

== 5.1 三工况聚合

单一载荷工况容易使结构对某一种受力方式过拟合。本文采用三组代表性载荷，分别完成正向骨改建和反向伴随分析。设第 `c` 个工况终态骨量为 `B_c`，则总体最小化目标为

$
J(x) = - (B_1(x) + B_2(x) + B_3(x)).
$

总体梯度为

$
nabla J(x) = - (nabla B_1(x) + nabla B_2(x) + nabla B_3(x)).
$

三工况可以共享同一设计变量和初始骨密度，但每个工况的载荷、位移、刺激因子、终态骨量和伴随变量独立计算。聚合只发生在目标函数、约束优化输入和最终设计更新阶段。

三工况权重在当前基础模型中取相同权重。若后续临床或实验数据表明某一类载荷更常见或更危险，可以把目标函数推广为加权形式：

$
J(x) = - sum_(c in C) w_c B_c(x), quad sum_(c in C) w_c = 1.
$

权重会直接改变梯度聚合结果。某个工况权重提高后，优化结构会更偏向该工况下有利于骨量增长的材料分布。正式实验应先使用等权重作为基线，再根据研究问题讨论是否需要加权。

三工况之间还可能存在竞争关系。某个设计单元在压缩工况下增加材料有利于目标骨量，在弯曲或扭转工况下却可能降低局部刺激。聚合梯度把这些竞争合并到一个方向上。结果分析中应分别报告每个工况的终态骨量和梯度贡献，避免只看总目标而忽略某个工况明显退化。

== 5.2 一次迭代的数据流

一次外层迭代需要保存足够信息，保证后续可以复查每个梯度项的来源。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr, 0.35cm, 1fr, 0.35cm, 1fr),
      stroke: none,
      smallflowbox([工况 1#linebreak()正向 `t=0..P-1`#linebreak()反向 `t=P-1..0`], fill: rgb("#eef6ff")),
      align(center)[#rightarrow],
      smallflowbox([工况 2#linebreak()正向 `t=0..P-1`#linebreak()反向 `t=P-1..0`], fill: rgb("#eef6ff")),
      align(center)[#rightarrow],
      smallflowbox([工况 3#linebreak()正向 `t=0..P-1`#linebreak()反向 `t=P-1..0`], fill: rgb("#eef6ff")),
      table.cell(colspan: 5)[#v(0.45em)],
      table.cell(colspan: 5)[#align(center)[#downarrow]],
      table.cell(colspan: 5)[#flowbox([聚合层：终态骨量、设计梯度、材料约束、约束梯度], fill: rgb("#fff6e8"))],
      table.cell(colspan: 5)[#align(center)[#downarrow]],
      table.cell(colspan: 5)[#flowbox([更新层：MMA 状态、下一轮设计、收敛指标、迭代记录], fill: rgb("#edf9f5"))],
    )
  ],
  caption: [一次三工况优化迭代的数据流。],
)

这张数据流图对应的实际计算顺序是：先固定设计变量，依次完成三个工况的正向和反向分析；再把三个工况的目标函数和梯度交给聚合层；最后由优化层产生下一轮设计。聚合层只读取已经完成的工况结果，不应修改单工况内部状态。这样可以保证单工况结果既能独立分析，也能参与总体优化。

每个工况内部还应保存时间步序列。正向序列保存 `b^t -> u^t -> SF^t -> b^(t+1)`，反向序列保存 `psi^(t+1) -> F_v^t -> lambda^t -> psi^t`。若后续发现某个工况结果异常，可以只重跑或检查该工况，而不必重新解释整个优化流程。这种分层记录对长时间大模型实验尤其重要。

== 5.3 验证层级

验证工作需要区分“文件存在”“数值有限”“链路闭合”和“实验结论可信”四个层级。文件存在只能说明运行产物已保存；数值有限说明没有出现 NaN 或 Inf；链路闭合说明正向、反向、聚合和优化步骤相互对应；实验结论可信还需要真实实验、误差分析和必要对照。

#figure(
  block(width: 100%, fill: rgb("#ffffff"), stroke: rgb("#b9c1c9"), radius: 3pt, inset: 8pt)[
    #table(
      columns: (1fr),
      stroke: none,
      flowbox([第 1 层：输入检查#linebreak()网格、区域集合、材料参数、载荷、边界条件], fill: rgb("#eef6ff")),
      align(center)[#downarrow],
      flowbox([第 2 层：正向检查#linebreak()位移非零、刺激有限、密度更新在物理区间内], fill: rgb("#f5fbf1")),
      align(center)[#downarrow],
      flowbox([第 3 层：伴随检查#linebreak()动态伴随载荷存在、伴随位移有限、反向时间顺序正确], fill: rgb("#f7f2ff")),
      align(center)[#downarrow],
      flowbox([第 4 层：优化检查#linebreak()目标、约束、梯度、MMA 状态和设计变化可追踪], fill: rgb("#fff6e8")),
      align(center)[#downarrow],
      flowbox([第 5 层：科学检查#linebreak()真实实验数据、误差对照、收敛分析和结果解释], fill: rgb("#edf9f5")),
    )
  ],
  caption: [骨改建拓扑优化验证层级。],
)

输入检查层关注模型是否被正确定义。它包括节点坐标范围、单元连接合法性、区域集合是否互相覆盖或冲突、设计单元和目标骨单元数量是否符合预期、材料参数是否为正、载荷方向和自由度是否明确。输入检查不能证明优化结果正确，但可以避免把错误模型带入后续计算。

正向检查层关注有限元和骨改建是否产生可信状态。位移需要非零且有限，单元能量不能出现异常负值，`SF` 应落在可解释范围内，骨密度更新应满足上下界。若某个时间步大面积触碰边界，需要在结果中解释这是物理饱和、参数设置还是数值问题。

伴随检查层关注反向链路是否真实依赖正向结果。动态伴随载荷应随时间步和工况变化，伴随位移应与载荷位置和刚度状态相匹配，`psi` 的时间递推应从终态向初态传播。该层最重要的外部验证是有限差分方向导数。只要梯度没有经过有限差分检查，伴随结果就只能视为内部一致证据。

优化检查层关注设计更新是否满足约束并具有可解释趋势。设计变量应保持在盒约束内，材料用量约束不应持续严重违反，`delta` 应随迭代总体下降，目标函数变化应与梯度方向相符。若目标函数震荡，需要检查 MMA 渐近线、梯度尺度和线性求解误差。

== 5.4 需要保存的关键证据

每轮迭代至少应保存以下证据：当前设计变量、下一轮设计变量、三工况终态骨量、每个时间步的骨密度、力学刺激因子、正向位移、动态伴随载荷、伴随位移、骨密度伴随变量、聚合梯度、约束值、MMA 渐近线状态和收敛指标。

这些证据的价值在于定位误差来源。若终态骨量异常，可以检查刺激因子和骨密度增量；若梯度异常，可以检查动态伴随载荷和伴随位移；若优化震荡，可以检查 MMA 渐近线、约束值和设计变化量。

保存证据时还应记录参数上下文。相同的设计变量在不同材料参数、时间步长、载荷大小或线性求解容差下会产生不同结果。每个运行目录应记录模型版本、参数表、载荷表、时间步设置、求解容差和随机种子。这样后续对比不同实验时，才能判断差异来自算法变化还是参数变化。

结果可视化也应服务于验证。位移云图用于检查载荷和约束，`SF` 分布图用于检查骨改建驱动，骨密度差值图用于展示组织响应，设计变量图用于展示优化结果，梯度图用于解释材料移动方向。每类图都应有对应的数值表，避免只凭图像判断。

== 5.5 从伪密度场到 Voronoi 骨架的制造化流程

前四节给出的仍然是“优化变量如何更新”的数学与审计框架。对植入物设计而言，这还不等于最终可制造结构，因为优化输出本质上是设计域上的相对密度场，而不是具有最小杆径、连通孔隙和明确边界的三维实体。为了把优化结果转化为可制造几何，本文在优化闭环之后追加一条下游几何流程：先把伪密度场变成概率采样种子点，再构造受限 Voronoi 单元，提取脊线骨架，最后把骨架实体化为 STL / GLB。

当前仓库中，这条流程已经由 `matlab2stl_pipeline` 和 `topopt_sampling` 两个节点实现。以 `iter_017` 为例，其后处理顺序可概括为：

$
text(伪密度场) -> text(对齐后的概率场) -> text(种子点采样) -> text(CVT 松弛) -> text(受限 Voronoi 单元) -> text(Voronoi 脊线) -> text(骨架体素化) -> text(STL / GLB).
$

这条流程的输入和输出都已被标准化保存。输入端包括 `fjw_iter017_density.npz` 与 `fjw_iter017_aligned_density_gamma1.npz`，中间产物包括 `fjw_iter017_seeds_200_gamma1.npz`、`fjw_iter017_seeds_200_gamma1_cvt500.npz`、`fjw_iter017_voronoi_density.npz` 和 `fjw_iter017_voronoi_edges_density.npz`，末端产物包括 `fjw_iter017_skeleton_voxels_density.npz`、`fjw_iter017_skeleton_density.glb` 与 `fjw_iter017_skeleton_density.stl`。因此，本文在这一节讨论的不是抽象设想，而是已经被当前工作流真实跑通的制造化链路。

但这里必须先把一个学术边界说清楚。本文中的伪密度场不是直接的几何参数场，它首先是设计区上的目标等效力学性能场。按照当前 FJW 实现，设计区单元模量由 `cage_objective_modulus()` 给出：

$
E_c(x_i) = E_(c,min) + E_(c,0) x_i^3, quad x_(min) <= x_i <= 1,
$

并在数值实现中截断到 `E_(c,0)`。因此，`x_i` 的首要含义不是“这里应当长成哪一种 Voronoi 图案”，而是“这里应当达到多大的等效刚度需求”。

这一步决定了下游几何链路不能只回答“高密度区域放更多 seeds”，还必须回答“最终钛合金 Voronoi 杆系怎样才在力学上等效表达这个目标模量场”。换句话说，真正需要建立的是

$
E_(eff)^(Voronoi)(theta_i) approx E_c(x_i),
$

其中 `theta_i` 表示第 `i` 个局部区域的 Voronoi 几何参数，例如杆径、局部 seed 密度、单元尺度和连接拓扑。没有这一步，几何只能说受伪密度分布趋势引导，还不能说力学上与伪密度设计等效。

把伪密度结果转成 Voronoi 骨架有三个直接原因。第一，伪密度场只定义了“哪里应保留更多材料、哪里应释放更多材料”，它并不直接提供可打印的边界表示。第二，Voronoi 杆系天然提供连通孔隙与杆件网络，适合作为多孔融合器的几何基元。第三，骨架体素化和 marching cubes 可以进一步导出 STL / GLB，使优化结果进入可视化、制造和后续有限元回代验证阶段。

这一流程的关键思想是把“材料分布信息”和“几何均匀性要求”分成两个阶段处理：先在概率采样阶段用密度权重保留优化结果中高密度区域更值得布置杆件的趋势，再在 CVT 阶段消除初始随机采样的团聚，使最终 Voronoi 单元尺寸和形状更稳定。这样做避免了把“局部高密度需要更多种子”和“几何上相邻种子应均匀铺开”混成同一个步骤。

但仅靠这两个阶段仍然不够。采样与 CVT 只能给出“几何分布趋势”和“几何质量控制”，不能自动给出“局部刚度恰好匹配目标模量”。因此，完整的下游制造化流程必须再补一层标定：先由伪密度插值得到目标等效模量，再由均匀化或小样本有限元标定，反求 Voronoi 几何参数。本文后续把这层称为“标定层”。

== 5.6 种子点采样与 CVT 松弛

在对齐后的伪密度场中，设体素索引为 `v`，密度值为 `rho(v)`。当前实现先构造采样概率

$
p(v) = frac(rho(v)^gamma)(sum_u rho(u)^gamma),
$

再对离散概率分布做逆变换采样，得到初始种子点集合。这里 `gamma` 用于调节高密度区域对采样概率的放大程度。`gamma = 1` 时，采样概率与密度成正比；`gamma > 1` 时，高密度区域更容易获得额外种子点；`gamma < 1` 时，概率场更平缓。`iter_017` 当前使用的是 `gamma = 1` 和 `200` 个种子点。

初始概率采样已经能够把更多种子点放到高密度区域，但它仍然存在明显随机性：局部区域可能出现种子点聚团，某些 Voronoi 单元过小，某些单元过大。若直接用这批 seeds 构造骨架，最终杆件长度、相邻单元体积和连通拓扑会出现较大波动，不利于制造和后续性能解释。因此，本文在采样之后引入 CVT 松弛。

本文当前使用的 CVT 算法是标准 Lloyd 迭代，其实现位置在 `src/matlab2stl_pipeline/cvt_relaxation.py`。每轮迭代执行四步：第一，把真实 seeds 关于边界盒的 6 个面做镜像扩展，以保证边界单元也拥有有界 Voronoi 区域；第二，对扩展后的点集计算三维 Voronoi 剖分；第三，对每个真实 seed 对应的 box-clipped Voronoi cell 计算几何中心；第四，用该几何中心更新 seed 位置，并裁剪回边界盒内部。

这里要特别说明本文为什么使用几何中心而不是密度加权中心。标准 CVT 的目标是使 Voronoi 单元在几何上趋于均匀，减少极端瘦长单元和局部拥挤。如果在松弛步骤继续使用密度加权中心，种子点会再次被拉回高密度区域，等于把采样阶段已经编码进来的密度偏置重复施加一遍，破坏几何均匀化的目的。因此，本文把“高密度区域布置更多种子点”放在采样阶段，把“使种子点局部均匀铺开”放在 CVT 阶段，两者职责分开。

为什么必须做 CVT，可以归纳为三个 `Why`。第一，初始概率采样只保证统计意义上的分布趋势，不保证局部单元质量；CVT 用来提升局部几何稳定性。第二，后续 Voronoi 脊线会直接决定骨架杆件的长度和连通关系，CVT 能降低极短边、极近点和局部退化单元的概率。第三，制造阶段更关心可控杆径、单元均匀性和连续孔隙，CVT 收敛后的点集通常比原始随机点集更适合用于实体化。

对应地，本文的三个 `How` 也很明确。第一，密度信息通过 `gamma` 指数概率场进入初始采样，而不是进入 Lloyd 更新公式。第二，边界处理通过镜像 seeds 与 box clipping 完成，避免边界 Voronoi 单元无界。第三，实际运行中采用固定 `500` 轮 Lloyd 迭代，并每隔若干步检查退化单元数量与最大位移变化；在 `iter_017` 的当前产物中，这一步输出为 `fjw_iter017_seeds_200_gamma1_cvt500.npz`。

这里还要补充一个 `What not`。CVT 的职责不是把种子点直接调成“局部刚度正确”。CVT 只负责改善单元质量和局部均匀性，不负责校准杆系的等效模量。若把 CVT 误当成力学等效步骤，就会把“几何均匀化”和“材料刚度匹配”两个完全不同的问题混为一谈。本文因此把它们严格拆开：`gamma` 负责密度驱动的空间偏置，CVT 负责几何均匀化，标定层负责局部力学等效。

== 5.7 受限 Voronoi 构造、脊线提取与骨架实体化

CVT 收敛后的 seeds 仍只是点集。要形成可制造结构，还需要把这些点集转成受限 Voronoi 单元，再进一步转成脊线骨架。当前实现先在对齐后的边界盒内构造每个 seed 的 Voronoi 单元，并用 box clipping 保证所有单元都被截断在有效区域内。这样得到的每个 cell 都以顶点、面环和凸包三角面片的形式保存，统一写入 `voronoi.npz`。

从受限 Voronoi 单元到骨架的关键步骤是提取所有单元面的边界线段。对于受限 Voronoi 复形而言，这些一维边界恰好构成本文使用的骨架脊线网络。当前代码在 `extract_voronoi_edges()` 中遍历各单元的有序面环，去重后输出 `edges : (E, 2, 3)`。对 `iter_017` 的当前产物而言，已经提取到 `2420` 条 Voronoi 脊线。

但这些脊线仍然只是数学线段，还不是有厚度的实体结构。为了把它变成可制造几何，本文继续执行体素化和实体化两步。第一步是把每条 Voronoi edge 栅格化到细分后的高分辨率体素网格中；第二步是在该细网格上做球形结构元素膨胀，把一维线段扩成具有有限杆径的实体骨架。当前实现默认把原始 `400 mu m` 体素沿各方向细分 `10` 倍，因此细体素分辨率约为 `40 mu m`，再在细体素网格上用半径 `3.0` 的球形结构元素做膨胀。

体素化后的实体骨架保存为 `skeleton_voxels.npz`，其中既包含占据体素数组，也包含真实物理尺寸、原点和膨胀参数。最后一步使用 marching cubes 从体素占据场中提取等值面，生成三角网格，再导出为 GLB 和二进制 STL。这样一来，优化的伪密度输出就被完全转换为可预览、可交付、可再体素化的实体骨架结构。

然而，到这一步仍然只是几何闭环，不是力学闭环。当前已经生成的 STL / GLB 只能证明“伪密度场被变成了可制造的实体骨架”，不能证明“这个骨架在钛合金材料下恰好具有伪密度场要求的局部刚度”。因此，本文不把当前这一版骨架直接称为“力学等效骨架”，而把它称为“几何上完成实体化的骨架原型”。

这一节的最终结论应限定为“几何制造化链路已经建立并已生成实际交付产物”。当前还不能仅凭 GLB / STL 就声称制造约束已经完全满足，也不能仅凭 seed 分布就声称局部刚度已经匹配，因为最小杆径、公差、打印方向、表面粗糙度、工艺窗口以及等效模量标定都还没有被单独量化；但至少可以确认，本文已经把抽象密度场推进到了实体骨架层面，而不是停留在颜色图或阈值图像阶段。

== 5.8 骨架回代有限元验证的目标与判据

把伪密度场转换为 Voronoi 骨架之后，仍然有一个关键问题没有自动成立：这个可制造骨架是否还保留了原始伪密度优化所追求的力学刺激效果。换句话说，几何链路成功只证明“结构被造出来了”，还不能证明“被造出来的结构仍然等效表达了原设计意图”。因此，本文把骨架回代有限元验证定义为下游制造化流程的核心验证实验。

该验证的基本思想不能再简单写成“把骨架替换进去再重算一次”。真正合理的验证顺序应当是两层。第一层，先验证局部标定关系，即伪密度插值得到的目标模量场能否被 Voronoi 几何参数反求并重现。第二层，再把带有正确局部参数分布的 Voronoi 骨架替换回完整设计域，重新计算三工况位移场和目标骨区刺激，并与原伪密度设计的响应作对比。只有这两层都成立，才能说制造化后的骨架结构保留了原始优化结果的主要力学效果；若任何一层失败，就说明几何转换过程中引入了不可忽略的性能漂移。

当前仓库已经具备这项验证所需的几何侧基础能力，但还没有形成完整自动化实验链。现有能力包括：一，最终骨架 STL / GLB 已经生成；二，`ct_reconstruction/voxelizer.py` 已经支持把闭合 STL 重新体素化；三，`fem_analysis` 已经支持对体素化设计域执行三工况有限元与骨改建计算。当前真正仍缺少的，不只是把三部分接成一条脚本，更关键的是补上一层“伪密度目标模量 -> Voronoi 参数 -> Voronoi 等效模量”的标定关系。没有这层关系，任何直接二值占据回代或 fill-fraction 回代都只能算探索性 dry run，不能算严格的学术验证。

本文建议的标定层可以有三种实现口径。第一种是局部均匀化标定：固定 Voronoi 风格，扫描杆径 `r`、单元尺度 `l` 和局部 seed 密度 `n`，通过小样本有限元得到 `E_(eff) = f(r,l,n,topology)`，再反求满足 `E_(eff) approx E_c(x)` 的参数组合。第二种是体积分数加经验模量关系：先用相对密度 `phi` 近似匹配伪密度，再通过 `E_(eff)/E_s approx C phi^m` 这类标定关系把几何占料连接到等效模量。第三种是直接局部刚度反求：直接以局部等效刚度矩阵匹配为目标调节杆径和 seed 密度。本文后续实验优先采用第一种，因为它最接近“目标模量场 -> 几何参数场”的正式定义。

因此，本文对这项验证采用三层表述。第一层是已经成立的几何链路：伪密度场已经被变成真实 Voronoi 骨架。第二层是当前必须补齐的标定层：局部目标模量和局部几何参数之间的映射尚未建立。第三层才是完整模型回代有限元验证：只有在标定层完成之后，回代比较结果才具有真正的物理解释。

这项实验的建议比较指标包括四类。第一类是整体力学指标：三工况最大位移、95 分位位移、关键节点位移以及位移场空间相关性。第二类是骨刺激指标：目标骨区 `bone_s` 的均值、分位数、峰值和空间投影相似性。第三类是骨改建响应指标：一步骨密度增量、目标骨量总和和各工况终态骨量变化。第四类是制造几何指标：杆件总体体积分数、连通性、最小局部厚度和 STL 可闭合性。只有把这四类指标联合起来，才能同时回答“力学效果是否保留”和“结构是否具备制造意义”。

目前已经完成的一次探索性 dry run 也恰好说明了这个问题。把当前骨架占据直接映射回设计区做二值回代后，`force_1` 的最大位移比值达到 `1.8248`，`bone_s` 均值比值达到 `2.5543`，目标骨区一步骨密度增量总和从参考设计的 `+37.3364` 变成 `-35.0384`，并且 `bone_density_delta` 相关系数仅为 `-0.0818`。这个结果不能被写成“骨架失败”，它更准确地说明：在没有标定层的前提下，直接用几何占据替换伪密度设计变量会造成严重的物理失配，因此这类 dry run 只能作为反例，用来证明标定层的必要性。

= 第 6 章 实验方案与预期结果

== 6.1 实验总体设计

完整实验尚未全部完成。本文当前阶段已经完成输入完整性验证、小模型公式验证，以及基于 `runs/fjw_optimize_real/iter_017` 的完整参考模型一轮三工况真实 checkpoint 整理；完整多轮收敛和外部黄金对照验证仍作为后续实验。第 6 章因此同时包含已运行结果和后续实验位置，所有尚未运行的数据继续以【待填/待确认】标记。

实验环境应记录 CPU 型号、内存容量、操作系统、求解器版本、线性求解配置、线程数、运行时间、峰值内存和代码版本。每组实验均应保存运行目录、验证报告、关键数组校验和、图像来源和参数配置。

当前能够从保留下来的运行产物中确认的实验环境如下：运行 profile 为 `wuyinyun`，后端为 `sfepy`，求解模式为 `three-force`，线性求解器为 `scipy_iterative`，工况级并行为 `2`，单工况数值线程配置为 `12`。`workflow_manifest.json` 还记录了数值线程环境变量 `OMP_NUM_THREADS=12`、`OPENBLAS_NUM_THREADS=12`、`MKL_NUM_THREADS=12`、`NUMEXPR_NUM_THREADS=12` 和 `VECLIB_MAXIMUM_THREADS=12`。运行日志路径显示 Python 运行时为 `cpython-3.13.13-linux-x86_64-gnu`，因此当前可确认执行环境属于 Linux `x86_64` 远端机器。

`iter_017` 的真实运行窗口可从 `runtime_events.jsonl` 读出：这一轮迭代在协调世界时 `2026-05-25T15:38:50.788946+00:00` 启动，在 `2026-05-25T16:57:35.300201+00:00` 完成 checkpoint 写出。对应的整轮耗时为 `4717.2634` 秒，其中三工况 `case_batch` 耗时 `4714.7982` 秒，checkpoint 写出耗时 `7.2475` 秒。当前保留的运行状态还证明该远端执行过程至少承受了 `34.2134` GB 峰值 RSS、`42.1480` GB 峰值 VMS 和 `584.1` MB 峰值 swap；因此这轮真实优化显然不是在当前本地 16 GB Mac 上完成的。

需要说明的是，当前运行产物没有保存远端机器的 CPU 型号和物理内存总量，所以这两项还不能写成确定值。当前阶段能够成立的表述是：论文已经具备可复查的远端 profile、操作系统类型、Python 与求解器版本、线程配置、运行日期和实际资源占用记录；精确硬件型号仍需后续从远端机器侧补录。

实验设计遵循由小到大的顺序。小模型负责验证公式和梯度，完整模型装配负责验证输入和边界条件，单工况求解负责验证大规模线性系统，三工况闭环负责验证优化流程，最终多轮优化负责生成论文结果。这样的顺序可以降低排错难度：若完整模型优化失败，可以回到单工况求解；若单工况求解异常，可以回到装配检查；若梯度方向异常，可以回到小模型有限差分验证。

每组实验都应区分“完成状态”和“结论强度”。例如，模型可以成功装配，只能说明输入和离散结构可用；单工况可以求解，只能说明给定载荷下有限元系统可解；三工况一轮闭环通过，只能说明算法链路可运行；多轮收敛并生成合理结构后，才可以讨论优化效果。本文当前版本保留待填标记，就是为了避免在缺少真实实验时提前给出强结论。

== 6.2 实验一：输入完整性与模型装配验证

实验目的在于确认网格、区域集合、材料参数、载荷和边界条件可以稳定读入，并能装配完整有限元问题。需要记录节点数、单元数、设计单元数、目标骨单元数、顶底约束节点数、参考点位置和三工况载荷表。这里的三工况载荷都施加在顶面参考点的 6 自由度刚性控制向量上，其中前 3 个分量对应平移力，后 3 个分量对应参考点力矩。

当前输入完整性检查显示，完整参考模型包含 `593790` 个节点和 `544112` 个六面体单元，体素网格尺寸为 `152 times 131 times 134`。设计单元数为 `18734`，目标骨单元数为 `5073`，融合器相关单元总数为 `23807`。皮质骨单元数为 `90207`，松质骨单元数为 `430098`。顶面节点数为 `3936`，底面节点数为 `5189`；参考点内部零基索引为顶面 `591312`、底面 `13705`，对应的一基节点号为顶面 `591313`、底面 `13706`。

#set par(first-line-indent: 0em)
#table(
  columns: (1.8cm, 3.1cm, 2.6cm, 4.0cm, 3.9cm),
  align: (center, center, center, left),
  [工况], [载荷向量], [主自由度含义], [物理解释], [装配状态],
  [`force_1`], [`[0, 0, -1200, 0, 0, 0]`], [`dof 3: z 向平移力`], [轴向压缩主导工况，参考点沿 `z` 方向受压并通过顶面刚性耦合传递到端面节点。], [有限元问题可构造，方程与刚性耦合注册完成],
  [`force_2`], [`[0, 0, 0, -7500, 0, 0]`], [`dof 4: 绕 x 轴力矩`], [绕 `x` 轴转动主导工况，对应俯仰/前后弯曲型载荷；`iter_017` 中其位移均值约 `0.0258`，低于 `force_3`。], [有限元问题可构造，方程与刚性耦合注册完成],
  [`force_3`], [`[0, 0, 0, 0, -7500, 0]`], [`dof 5: 绕 y 轴力矩`], [绕 `y` 轴转动主导工况，对应侧向弯曲/滚转型载荷；`iter_017` 中位移峰值约 `0.1861`，是三工况中响应最强的一组。], [有限元问题可构造，方程与刚性耦合注册完成],
)
#set par(first-line-indent: 2em)

按照 `datasets/fjw_abaqus_inputs.json` 与 `iter_017` 正向结果的联合整理，`force_1` 的平均位移向量在 `z` 方向呈明显负值，符合轴向压缩主导的解释；`force_2` 的平均位移在 `y` 方向更明显，而 `force_3` 的平均位移在 `x` 方向更突出，同时其峰值位移最大。因此，当前三工况可以稳定区分为“一组轴向压缩 + 两组正交转动主导载荷”，这为后续三工况聚合优化的物理含义提供了可复查的载荷解释基础。

材料组检查结果如下。四个材料组均能映射到单元集合，没有出现目标骨区缺材料、设计区缺材料或材料组空集合的问题。

#set par(first-line-indent: 0em)
#table(
  columns: (3.2cm, 2.6cm, 2.8cm, 2.4cm, 4.0cm),
  align: (center, center, center, center, left),
  [材料组], [单元数], [弹性模量], [泊松比], [含义],
  [`BONE_COR`], [`90207`], [`18000.0`], [`0.3`], [皮质骨],
  [`BONE_TRA`], [`430098`], [`1200.0`], [`0.3`], [松质骨],
  [`CAGE_BUCKET_1`], [`18734`], [`1100.0`], [`0.3`], [可设计融合器区域],
  [`BONE_BUCKET_0`], [`5073`], [`12.0`], [`0.3`], [目标骨区初始材料],
)
#set par(first-line-indent: 2em)

初始设计变量在全部 `18734` 个设计单元上取 `0.2`，初始设计变量总和为 `3746.8`。目标骨区初始骨密度范围为 `0.36` 到 `0.36`。本轮验证的结论是：节点、单元、材料组、载荷向量、顶底约束集合和参考点耦合定义均可被读入并形成三工况有限元装配输入。装配时间、峰值内存和完整线性系统求解状态留到完整参考模型求解实验中记录。

输入完整性实验还应检查区域集合之间的关系。皮质骨、松质骨、融合器、设计区域和目标骨区都需要明确数量和空间位置。设计区域应属于融合器可调范围，目标骨区应属于需要评价骨长入的骨组织范围。若某个设计单元同时被标为目标骨单元，或者某个目标骨单元缺少材料定义，都会破坏模型含义。

模型装配验证应报告全局自由度数量、受约束自由度数量、载荷自由度数量和材料组数量。对于参考点耦合，还应记录参考点坐标、参与耦合的顶面节点数量和耦合自由度。装配成功后，可以先不求解完整线性系统，只检查刚度矩阵形状、非零元数量和右端载荷范数。这样可以在正式求解前发现规模、约束或载荷问题。

本实验的预期结论应写得谨慎。若只完成装配，应表述为“完整参考模型能够形成有限元离散问题”；若完成单工况求解，才可以表述为“该工况下有限元方程可求解并产生有限位移”。论文结果章节应避免把装配成功直接写成优化成功。

#figure(
  image("../../Post process/figure/61/output/6-1-reference-region-layout.png", width: 100%),
  caption: [完整参考模型区域分布图。],
)

== 6.3 实验二：小模型公式验证

小模型用于验证公式链路。建议构造单个或少量六面体单元，给定明确材料参数、载荷、初始骨密度和设计变量。通过手算或高精度数值计算对比 `K_e`、`q_e`、`SF`、`R(SF)`、`b^(t+1)`、动态伴随载荷和设计梯度。

评价指标包括最大位移、单元能量、力学刺激因子、骨密度增量、终态骨量、伴随载荷范数、伴随位移最大值、设计梯度和有限差分误差。

本轮小模型验证采用非零位移和非零骨改建响应，直接检查力学刺激因子、骨密度更新和材料插值导数。该算例中，单元二次能量项 `q_e = 0.030000000000000006`，目标材料模量 `E_obj = 0.146656`，力学刺激因子 `SF = 0.012221333333333338`，骨密度增量为 `0.013401830369778241`，更新后骨密度为 `0.37340183036977825`。动态伴随载荷有 `2` 个活跃节点，向量范数为 `0.03919950609957225`，说明该公式检查属于非零响应算例。

#set par(first-line-indent: 0em)
#table(
  columns: (3.8cm, 3.2cm, 3.6cm, 3.2cm),
  align: (center, center, center, center),
  [检查量], [解析值], [有限差分值], [绝对误差],
  [`d SF / d u_0`], [`0.04073777777777779`], [`0.04073777777786891`], [`9.11e-14`],
  [`d R / d SF`], [`0.3928327028409428`], [`0.3928327028399417`], [`1.00e-12`],
  [`d E_c / d x`], [`0.54`], [`0.5399999999933236`], [`6.68e-12`],
)
#set par(first-line-indent: 2em)

有限差分步长取 `1e-4` 时，上表三个关键导数的绝对误差均低于 `1e-11`。这说明当前实现中的刺激因子导数、骨改建函数导数和设计材料插值导数与公式推导一致。

另外还运行了一个三工况最小链路的目标函数方向检查。该检查的初始设计变量为 `0.3`，目标函数为 `-1.059`，伴随梯度为 `0.0`，有限差分梯度为 `0.0`，约束 `g_2 = 0.0`，一次更新后的设计变量为 `0.22894406338880258`，设计变化量 `delta = 0.07105593661119741`。该最小链路的目标骨刺激项为零，因此它只证明三工况目标、约束和优化更新的数据链一致；非零导数证据以上表的公式检查为准。完整参考模型的一轮真实指标则放在后面的 `iter_017` 实验中单独报告。

小模型应至少包含两类场景。第一类是单单元或少量单元的解析场景，用于检查单元刚度、能量项、`SF` 和骨密度更新。该场景可以设置简单位移或简单载荷，使手算结果清晰。第二类是包含设计单元和目标骨单元耦合的场景，用于检查设计变量通过刚度影响目标骨密度的链式关系。该场景不追求临床真实，只追求导数路径完整。

有限差分步长 `epsilon` 需要选择合理。步长过大时，有限差分会受到非线性高阶项影响；步长过小时，浮点舍入误差会放大。实验中可以选取多个 `epsilon`，观察误差是否呈现先下降后上升的趋势。若伴随方向导数与有限差分在一段稳定步长范围内一致，梯度可信度较高。

小模型还可以用于验证分段骨改建函数。通过人为设置不同刺激水平，使单元分别落入低刺激吸收区、惰性区、成骨区和过高刺激吸收区，检查 `R(SF)` 和 `R'(SF)` 的值是否符合表格定义。这个实验能直接支持第 2 章的骨改建函数推导。

#figure(
  image("../../Post process/figure/62/output/6-2-minimal-forward-vs-adjoint-load.png", width: 100%),
  caption: [小模型正向位移与伴随载荷示意。左侧为正向位移，右侧为由当前伴随公式生成的节点载荷。],
)

== 6.4 实验三：三工况闭环 smoke test

三工况闭环实验用于确认完整参考模型的一轮外层迭代能够完整执行。当前论文使用 `runs/fjw_optimize_real/iter_017` 作为稳定 checkpoint，围绕其保存的一轮正向、伴随、聚合与 MMA 更新产物进行结果整理。

预期输出包括三工况终态骨量、目标函数 `J`、材料约束 `g`、设计变化量 `delta`、设计变量范围、动态伴随载荷范数、伴随位移最大值和验证报告摘要。

本轮结果整理使用的运行目录为 `runs/fjw_optimize_real/iter_017`。当前设计变量范围为 `0.0012092355666352657` 到 `0.9995315027753876`，均值为 `0.2912715976140572`；一次更新后范围为 `0.0010001660153491688` 到 `0.9999997379552096`，均值为 `0.2836738437740036`，设计变化量 `delta = 0.043310858427029836`。三工况终态骨量之和分别为：`force_1 = 1102.2164428406536`，`force_2 = 1074.2028551424678`，`force_3 = 1088.497131841567`；相对于初始 `1064.88`，三者分别增加 `37.336442840653945`、`9.322855142468143` 和 `23.617131841567243`。聚合目标函数 `J = -3264.9164298246883`，材料约束 `g_2 = -163.5178902982525`。

#set par(first-line-indent: 0em)
#table(
  columns: (2.6cm, 2.2cm, 2.2cm, 2.2cm, 4.4cm),
  align: (center, center, center, center, left),
  [验证状态], [pass], [warn], [fail], [说明],
  [`fail`], [`365`], [`1`], [`155`], [失败主要来自 `iter_003` 到 `iter_016` 的半同步 checkpoint 缺文件，以及 `iter_002/force_3/forward_t0/forward_step.npz` 缺失；单独整理过的 `iter_017` 本身 forward / adjoint 产物齐全。],
)
#set par(first-line-indent: 2em)

当前 `runs/fjw_optimize_real/validation_report.json` 的总体状态是 `fail`，但这个失败并不是 `iter_017` 正向或伴随链路本身失效，而是因为同一运行根目录下更早的 `iter_003` 到 `iter_016` 含有中断 rsync 留下的半同步目录，另外 `iter_002` 缺少一个 `force_3/forward_t0/forward_step.npz`。在把 `iter_017` 单独拿出来检查时，`iteration_state.json`、`aggregate_terms.npz`、三个工况的 `case_history.npz`、`forward_step.npz`、`fai.npz` 和 `fv_manifest.json` 都完整存在，因此本节结论应限定为：“完整参考模型的一轮三工况闭环真实运行产物已经形成，并且 `iter_017` 这一轮的数据证据链完整；整目录 validation 失败主要来自更早半同步 checkpoint，不能直接解释为 `iter_017` 失效。”

三工况闭环实验的重点是完整性。由于只运行一轮或少量时间步，目标函数可能还没有明显下降，设计变量也可能只发生小幅变化。该实验需要证明：三个工况都能产生正向位移，骨密度可以更新，动态伴随载荷可以生成，伴随位移可以求解，聚合梯度可以进入 MMA，并输出满足盒约束的下一轮设计。

结果表应按工况分别列出终态骨量和伴随载荷范数，再给出聚合后的目标、约束和设计变化。若某个工况的伴随载荷范数显著小于其他工况，需要检查该工况的 `SF` 是否处于惰性区、载荷是否过小或目标骨区是否没有受到有效刺激。若某个工况梯度支配总体方向，需要在讨论中说明该工况的力学意义。

该实验还应检查运行目录结构。每个工况应有正向时间步和反向时间步记录，聚合层应有目标和梯度记录，优化层应有 MMA 状态记录。目录结构检查不能替代数值验证，但可以确认数据证据链完整。

#figure(
  image("../../Post process/figure/63/output/6-3-three-force-iteration-metrics.png", width: 100%),
  caption: [三工况一轮优化指标变化。展示目标骨量变化、伴随载荷范数、位移响应量级以及聚合指标摘要。],
)

除了单独整理 `iter_017` 之外，当前仓库中还有少量更早迭代目录具备足够完整的 `iteration_state.json`、`design_cage.npz`、`aggregate_terms.npz` 和三工况 `case_history.npz`，可以用于构造一条“当前可用真实迭代趋势摘要”。按照 `Post process/analysis/output/available_iterations_summary.json` 的整理结果，当前可直接纳入趋势分析的目录包括 `iter_001`、`iter_002`、`iter_003`、`iter_004` 和 `iter_017`；`iter_005` 之后到 `iter_016` 由于缺少完整 case 产物或 checkpoint 半同步，不应直接当作连续收敛序列使用。

这条可用趋势的含义需要严格限定。它不能证明完整优化已经连续收敛，也不能替代多轮稳定实验；它只说明当前仓库里已经保留下来一组可复查的真实迭代切片，并且这些切片在目标函数、设计变化量和三工况终态骨量上形成了可量化的历史轨迹。对于当前论文版本，这比完全没有多轮证据更强，但结论强度仍应低于正式的连续收敛实验。

#figure(
  image("../../Post process/figure/69/output/6-9-available-iteration-trends.png", width: 100%),
  caption: [当前可用真实迭代趋势摘要。展示当前仓库中足够完整、可直接纳入分析的迭代目录在目标函数、设计变化量、终态骨量和设计均值上的变化。],
)

为了把这个限制说得更严格，当前还需要把多轮目录本身的完整性显式画出来。按照 `Post process/analysis/output/iteration_completeness_summary.json` 的扫描结果，`iter_001`、`iter_002`、`iter_003`、`iter_004` 和 `iter_017` 这五轮具备完整的顶层 checkpoint、三工况 `case_history`、`forward_step` 和 `adjoint` 产物；`iter_005` 只保留了部分 `force_1` 与 `force_2` 数据；`iter_006` 到 `iter_016` 则基本只剩空目录或半同步残留。这样一来，当前论文就能明确解释为什么“现有真实趋势”只能写成离散切片摘要，而不能写成连续收敛证明。

图 6-15 的作用不是展示优化效果，而是展示证据边界。它把每一轮 `iter_*` 目录里哪些关键产物存在、哪些缺失直接可视化，因此可以支持第 6 章对 validation 失败来源和中间轮次不可用原因的解释。对于学术写作，这种做法比笼统说“目录不完整”更可靠，因为读者能直接看到是哪一类证据缺失。

#figure(
  image("../../Post process/figure/75/output/6-15-iteration-completeness-map.png", width: 100%),
  caption: [多轮 checkpoint 完整性分布图。展示 `runs/fjw_optimize_real/iter_*` 中顶层 checkpoint 与三工况 forward / adjoint 关键产物的存在性，以及各轮目录的整体完整度。],
)

== 6.5 实验四：完整参考模型求解性能

完整参考模型求解是后续重点实验。该实验应先完成单工况有限元求解，记录线性系统规模、收敛状态、迭代次数、运行时间、峰值内存、最大位移、参考点位移和参考点转角。随后扩展到三工况，再扩展到多时间步骨改建和完整优化。

该实验的主要风险来自线性系统规模和内存占用。若默认开源线性求解配置收敛缓慢，可引入预条件或可选高性能线性代数后端，但论文结论应区分“模型可求解性”和“求解效率”。

当前已经可以从 `iter_017/timing.json`、三个 `forward_step.npz`、heartbeat 日志以及 `runtime_events.jsonl` 中整理出一轮真实运行的基础性能统计。外层迭代总耗时为 `4717.263390534557 s`，其中三工况 case batch 耗时 `4714.798244418576 s`，iteration aggregate 耗时 `2.4632412288337946 s`，checkpoint 写回耗时 `7.2475386913865805 s`。这说明当前一次真实迭代的主要成本几乎全部集中在三工况求解本身，而不是聚合或落盘。

同一批日志还给出了完整模型的线性系统规模。当前 SfePy 组装出的稀疏矩阵结构非零元数量为 `135005193`，填充率约 `4.33e-03%`；总自由度为 `1765809`，其中自由自由度 `1754001`，约束自由度 `11808`，active 自由度同样为 `1754001`。这些数字说明当前完整参考模型已经进入百万级自由度、上亿结构非零元的求解区间，求解开销主要受大规模稀疏线性系统驱动。

内存方面，`runtime_events.jsonl` 在 `iteration:017` 开始到 checkpoint 完成之间记录到的峰值 RSS 为 `31031934976` bytes，约 `28.90 GiB`；峰值虚拟内存 `39760056320` bytes，约 `37.03 GiB`；峰值 swap `584065024` bytes，约 `0.54 GiB`；峰值线程数 `22`。因此，当前这条真实闭环链路已经具备一轮完整性能画像：求解时间约 78.6 分钟，内存峰值接近 29 GiB，开销主体在三工况 forward / adjoint 求解阶段。

#set par(first-line-indent: 0em)
#table(
  columns: (2.5cm, 2.5cm, 2.8cm, 2.8cm, 2.8cm),
  align: (center, center, center, center, center),
  [工况], [终态骨量], [位移最大值], [位移均值], [位移 95 分位],
  [`force_1`], [`1102.2164428406536`], [`0.08379172826877591`], [`0.03033416784499487`], [`0.0703382362320757`],
  [`force_2`], [`1074.2028551424678`], [`0.09237054510283979`], [`0.025832520320201908`], [`0.07039169466231672`],
  [`force_3`], [`1088.497131841567`], [`0.1861271842395836`], [`0.03623472515066757`], [`0.1046845600905116`],
)
#set par(first-line-indent: 2em)

从位移统计看，`force_3` 的位移峰值约为 `0.1861`，明显高于 `force_1` 和 `force_2`，对应图 6-4 中也能观察到更强的非对称响应。`force_1` 与 `force_2` 的 95 分位位移都约为 `0.0703` 到 `0.0704`，而 `force_3` 达到 `0.1047`。这说明当前三工况并非简单等幅响应，而是已经体现出不同载荷模式对结构变形强度的区分。

需要明确的是，这一节虽然已经整理出一轮真实迭代的时间、位移量级、自由度规模、矩阵非零元数量和内存峰值，但仍然缺少单工况残差曲线、线性求解迭代次数时间序列以及多轮优化收敛数据，因此还不能写成完整性能评估。当前能成立的表述是：完整参考模型已经成功跑出一轮真实三工况 forward / adjoint / MMA 闭环，并且其主要耗时、规模和内存占用都已经可量化。

相比前一版草稿，当前还可以把单工况求解轨迹从日志中直接抽出来。`Post process/analysis/output/iter017_solver_trace_summary.json` 已经把 `iter_017` 对应的三工况 forward 日志块整理为残差序列、每轮 `matrix/solve` 耗时，以及 `runtime_events.jsonl` 中记录的 forward / adjoint 总耗时。按这份摘要，三工况 forward 求解都以 `7.500000e+03` 的初始残差启动，第一轮线性求解后分别下降到约 `2.83e+01`、`3.01e+01` 和 `2.78e+01`，量级上都进入 `10^1` 区间。与此同时，单轮 `solve` 时间都接近 `9.3e2` 到 `9.6e2` 秒，说明当前大模型性能瓶颈确实集中在线性求解阶段，而不是材料更新或数据落盘。

这类求解轨迹证据的价值在于，它把“完整模型能跑通”进一步推进到“完整模型在真实日志里表现出怎样的残差和求解时间结构”。当前仍然不能把这张图解释成严格收敛证明，因为日志只覆盖一轮真实 checkpoint，且没有更长的单工况时间序列；但它已经足够支持论文对求解成本和数值行为的定量描述。

#figure(
  image("../../Post process/figure/76/output/6-16-iter017-single-case-solver-traces.png", width: 100%),
  caption: [iter_017 单工况残差与求解时间轨迹。展示三工况在真实 heartbeat 日志中的残差序列、matrix/solve 时间和 forward / adjoint 总耗时。],
)

完整模型实验应分阶段推进。第一阶段只构建刚度矩阵和载荷向量，记录矩阵规模与稀疏度。第二阶段求解单工况线性系统，记录迭代次数和残差收敛。第三阶段计算目标骨区 `SF` 分布和一次骨密度更新。第四阶段加入伴随求解。第五阶段执行一轮或多轮三工况优化。每一阶段通过后再进入下一阶段，可以避免把多个问题叠加在一起。

性能指标应包含时间和内存两个维度。时间指标包括装配时间、线性求解时间、骨改建更新时间、伴随载荷生成时间和优化器更新时间。内存指标包括矩阵存储、位移向量、单元位移缓存和时间步历史。对于大模型，位移和伴随位移的保存策略会显著影响存储需求，因此实验中应记录哪些数组完整保存，哪些数组只保存统计量或校验和。

完整模型位移云图需要与载荷方向一致。压缩工况应显示合理的轴向位移分布，弯曲或扭转工况应显示对应方向的非对称响应。若云图与载荷含义明显不符，应优先检查参考点耦合、坐标单位和载荷自由度。骨密度变化图则应与 `SF` 分布共同解释，说明哪些区域获得成骨响应，哪些区域出现吸收或保持不变。

#figure(
  image("../../Post process/figure/64/output/6-4-full-model-displacement-clouds.png", width: 100%),
  caption: [完整模型位移云图。展示三个工况下的位移最大投影和最大位移位置。],
)

当前已经可以把 `iter_017` 的目标骨区前后骨密度变化重新映射回完整参考模型网格。具体做法是利用 `objective_elements` 的空间锚点，把三个工况 `forward_step.npz` 中保存的 `obj_bo_previous`、`obj_bo_next` 和 `bone_density_delta` 嵌回目标骨区，再做沿 `y` 方向的最大投影。这样可以直接观察三工况下目标骨区在一次 forward 更新前后的密度分布，以及哪些位置获得了正增量、哪些位置变化较弱。

这张图的结论强度仍应限定在“一轮真实更新的空间分布证据”。它说明当前骨改建更新不是只体现在全局骨量标量上，而是已经能在空间上区分不同载荷工况对目标骨区的影响范围和强弱；但它还不能替代多时间步时序图，也不能单独证明最终优化结构的长期骨生长趋势。

#figure(
  image("../../Post process/figure/70/output/6-10-objective-bone-density-change-maps.png", width: 100%),
  caption: [iter_017 目标骨区前后骨密度空间变化。展示三个工况在一次真实更新中目标骨区的更新前密度、更新后密度和密度增量投影。],
)

当前还可以进一步把三工况的设计灵敏度和聚合后的目标梯度重新映射回设计区。具体做法是利用 `design_anchor_indices` 把三个 `forward_step.npz` 中的 `design_sensitivity` 以及 `aggregate_terms.npz` 中的 `d_ob` 写回设计区空间，再做沿 `y` 方向的投影。这样不仅能看到目标骨区如何变化，还能直接看到伴随法给出的“哪些设计位置更值得保留材料、哪些位置更新驱动力较弱”。

这张图的作用是把“伴随法可计算”推进到“伴随法给出的更新方向具有明确空间分布”。它能够证明当前 `iter_017` 不只是算出了一个标量梯度，而是已经得到完整设计区上的空间灵敏度场；但它仍然不能替代多轮优化之后的最终结构对比，也不能单独证明设计更新已经达到最优。

#figure(
  image("../../Post process/figure/71/output/6-11-design-sensitivity-maps.png", width: 100%),
  caption: [iter_017 设计灵敏度空间分布。展示三个工况的 `design_sensitivity` 以及聚合目标梯度 `d_ob` 在设计区上的空间投影。],
)

当前还可以把 MMA 这一步真正对设计变量做了什么重新映射回设计区。`iter_017/design_cage.npz` 中同时保存了更新前的 `design_cage` 和更新后的 `next_design`，因此可以直接计算 `next_design - design_cage`，再把它写回设计区空间。这样得到的图不仅说明梯度场存在，还说明这轮优化实际把哪些区域往增材方向推、哪些区域往减材方向推，以及更新幅度主要集中在哪些位置。

这张图把证据链继续向后推进了一步。图 6-11 说明伴随法已经产生空间灵敏度，图 6-12 则说明 MMA 已经依据这些灵敏度和约束信息给出空间分布明确的设计更新结果。当前结论仍应限定在“一轮真实更新已经完整闭环”，因为这张图还不能替代多轮收敛曲线，也不能单独证明最终设计已经稳定。

#figure(
  image("../../Post process/figure/72/output/6-12-design-update-maps.png", width: 100%),
  caption: [iter_017 设计变量更新量空间分布。展示更新前设计变量、更新后设计变量、带符号更新量和相对更新幅度在设计区上的空间投影。],
)

当前 stable `iter_017` checkpoint 在每个工况下还保留了 `case_history.npz`，其中已经能读出当前可用的时间步骨量历史。虽然这一版真实 checkpoint 只有两个时间步可直接引用，即更新前初始状态和第一次真实骨改建更新后的状态，但这已经足够把“骨密度更新随时间推进”从单纯口头描述推进到可复现图表。进一步说，这里展示的是“当前可用时间步摘要”，它明确反映现阶段证据的边界，不把两步数据误写成完整长时程收敛。

图 6-13 的价值在于补上时间维度。图 6-10 强调空间分布，图 6-12 强调设计更新，图 6-13 则把目标骨区总量和分位统计沿时间步展开。它能够说明三个工况在第一次真实更新后都产生了可测的骨量上升，其中 `force_1`、`force_2` 和 `force_3` 的目标骨量总和分别从 `1064.88` 增加到 `1102.2164`、`1074.2029` 和 `1088.4971`。当前仍然不能把这张图解释成长期骨改建趋势，因为时间步数量还不够长。

#figure(
  image("../../Post process/figure/73/output/6-13-bone-density-time-step-summary.png", width: 100%),
  caption: [iter_017 当前可用骨密度时间步摘要。展示三个工况在当前 stable checkpoint 中可直接引用的目标骨量总和与目标骨区密度分位统计时间步历史。],
)

在优化结果展示层面，当前还可以再向前走一步：把后处理链路生成的最终三维结构以更高保真的方式直接展示出来。为此，图 6-14 以 `fjw_iter017_skeleton_voxels_density.npz` 为体素输入，通过 marching cubes 重新构造表面，再结合最终 `fjw_iter017_skeleton_density.stl` 的交付文件头读取三角面数和文件体积。这样做的目的很直接，就是把“已经得到最终几何产物”落实成一张能重复生成、并且能与最终交付 STL 对上的三维结果图。

这张图补的是第 6 章里“最终优化结构展示的更高保真版本”这一类证据。它证明从 `iter_017` 出发的后处理已经确实生成可交付的三维结构，并且这个结构不是停留在二维投影或粗粒度体素预览上。当前仍然不能把这张图写成最终制造质量证明，因为它没有覆盖网格质量、最小杆径、公差和打印可制造性分析。

#figure(
  image("../../Post process/figure/74/output/6-14-final-optimized-structure-hires.png", width: 100%),
  caption: [iter_017 最终优化结构高保真三维展示。基于最终骨架体素重建的高保真表面视图，并标注最终 STL 的三角面数与文件规模。],
)

== 6.6 实验五：Voronoi 骨架回代有限元验证

仅仅把伪密度场转换成 Voronoi 骨架，并不能自动证明这条制造化流程是成功的。真正需要验证的是：经过几何重建后的可制造骨架，是否仍然保留了原伪密度优化结果对目标骨区刺激分布的主要设计意图。因此，本节把“Voronoi 骨架回代有限元验证”定义为第 5 章下游几何链路的核心验证实验。

实验目标可以写成一句话：先建立伪密度目标模量场到 Voronoi 几何参数场的标定关系，再用带有正确局部参数分布的最终 Voronoi 骨架替换原始伪密度设计表示，重新进行三工况有限元与骨改建正向分析，并将其与原始 `iter_017` 伪密度设计的位移场、目标骨区刺激和一步骨密度响应进行逐项对比。若二者结果接近，则说明本文的“伪密度优化 -> 标定层 -> Voronoi 骨架制造化”流程在力学意义上成立；若差异显著，则说明几何转换过程中引入了不可忽略的性能漂移。

从现有代码基础看，这项实验的几何侧准备已经具备。最终骨架已经导出为 `fjw_iter017_skeleton_density.stl`、`fjw_iter017_skeleton_density.glb` 和 `fjw_iter017_skeleton_voxels_density.npz`；`ct_reconstruction/voxelizer.py` 支持把闭合 STL 重新体素化；`fem_analysis` 则已经具备在六面体体素设计域上执行三工况有限元和骨改建正向分析的主链。当前最关键的缺口不是 STL 回体素本身，而是尚未建立“局部目标模量 -> 局部 Voronoi 参数 -> 局部等效模量”的标定关系。没有这层关系，后续材料替换和三工况重算都缺少清晰物理意义。

本实验的建议流程分为五步。第一步，根据第 2 章中的材料插值关系，把原始伪密度场转换为目标等效模量场。第二步，在代表性局部 Voronoi 单元上扫描杆径、单元尺度和 seed 密度，建立 `E_(eff) = f(r,l,n,topology)` 的标定数据库。第三步，在完整骨架生成阶段依据局部目标模量反求几何参数，并将这些参数附着到 Voronoi 骨架上。第四步，从最终骨架 STL 或骨架体素结果生成与原始设计域同坐标系、同体素尺度或可稳定映射的占据网格，并赋予正确的局部几何参数或等效材料。第五步，在不改变三工况载荷、边界条件和目标骨区定义的前提下，重新执行正向有限元与一步骨改建更新，再将骨架回代结果与原始 `iter_017` 的伪密度设计结果做指标比较。

比较指标至少应覆盖四类。第一类是整体力学指标，包括最大位移、95 分位位移和关键节点位移。第二类是目标骨区刺激指标，包括 `bone_s` 的均值、分位数、峰值以及空间投影相关性。第三类是骨改建响应指标，包括一步骨密度增量统计、目标骨量总和和各工况的更新后目标骨总量。第四类是几何与制造指标，包括占据体素数量、等效体积分数、连通性和最小局部厚度。只有当这四类指标共同给出可接受结果时，才能说最终可制造骨架既保留了原始优化设计意图，又具备交付价值。

这项实验的判断标准也需要在论文中明确给出。第一层判断标准是标定层本身：局部 Voronoi 单元的等效模量应在声明误差带内逼近目标模量。第二层判断标准才是完整结构响应：三工况位移量级保持同数量级，目标骨区 `bone_s` 空间分布保持较高相关性，一步目标骨量响应处于预先声明的误差带内，并且不出现局部完全失支撑、刺激显著塌缩或载荷响应畸变等失效现象。后续若需要更严格论证，还可以继续引入各向异性等效刚度矩阵匹配或二次优化回代，但这属于扩展工作，不应替代当前的第一性验证。

当前阶段，这项实验的“方法定义”和“执行边界”已经明确，但真实比较结果还不能直接进入论文结论。其原因已经不再是“完全没有标定层”，而是“标定层只完成了第一版局部半径标定与第一版整结构代理回代，真实三工况 compare 结果尚未返回”。当前一次探索性二值回代表明，若跳过标定层，`force_1` 工况下的骨刺激与骨密度增量会出现明显漂移，甚至连骨密度增量总和的符号都发生反转。因此，本节当前能成立的结论应严格限定为：本文已经建立了从伪密度优化到可制造 Voronoi 骨架的完整几何链路，并已经把“均匀化/标定层”落实为一条真实的局部 FE 标定路径；骨架与伪密度在目标骨刺激上的定量等效性，还需要在第一版 `modulus_weighted` 回代的真实三工况重算结果返回后继续证明。

当前工作已经补出了标定层的第一批真实局部有限元结果，并且这批结果已经从两点 smoke 扩展到一组更宽的远端半径扫描。以 `4.0 mm` 物理单元尺寸、固定局部 seed realization、`0.1 mm` 体素尺寸和 `200 N` 单轴压缩为例，当前稳定样件的实测结果包括：`r = 0.08 mm` 时表观模量约 `5.01 GPa`，`r = 0.10 mm` 时约 `16.74 GPa`，`r = 0.12 mm` 时约 `22.44 GPa`，`r = 0.20 mm` 时约 `47.47 GPa`，`r = 0.24 mm` 时约 `57.33 GPa`，`r = 0.30 mm` 时约 `64.99 GPa`。这说明“杆径增大 -> 等效刚度显著提高”的关系已经被本仓库中的真实 FE 路径验证，标定层不再只是概念性设想。

同时，这批结果也给出了一个同样重要的负结论：`r = 0.06 mm` 与 `r = 0.16 mm` 的局部样件在当前固定拓扑和固定 seed realization 下出现了位移爆炸，表观模量分别退化到约 `5.12 times 10^(-8) GPa` 和 `4.19 times 10^(-11) GPa`，因此这两个采样点不能被纳入反求标定支撑。换句话说，当前这条 `r -> E_(eff)` 曲线并不是“半径越全越好”，而是已经出现了需要显式剔除的失稳区间。论文必须把这一点写清楚，否则后续 `E_(target) -> r_eff` 反求会把无效样本错误地当成可插值支撑。

但这批结果仍然还不能直接写成完整的 `rho(x) -> r(x)` 反求表。原因同样需要在论文中交代清楚：本轮局部样件对六个目标模量 band 仍然使用同一组局部 Voronoi 拓扑和同一组 seed realization，因此 band 之间当前只记录了不同的 `E_(target)` 标签，尚未驱动不同的局部几何参数。换句话说，当前已经得到的是第一版稳定区间内的 `r -> E_(eff)` 实测曲线，而不是最终的 `E_(target) -> r_eff` 标定数据库。后续还需要继续扩展高模量端的半径扫描范围，并把目标模量 band 真正映射到不同半径或其他局部参数，才能进入完整骨架回代有限元验证。

在这条稳定 `r -> E_(eff)` 曲线基础上，本文已经可以构造第一版 bandwise 反求基线。用稳定采样点做分段线性反插值后，`iter_017` 的前四个代表性目标模量 band 都会落到当前最小稳定采样半径 `0.08 mm`，第五个 band 对应约 `0.1635 mm`，最高模量 band 则直接饱和到当前最大稳定采样半径 `0.30 mm`。这个结果不能被误读成“完整标定已经完成”，它更准确地说明了两件事：第一，当前 `radius-only` 标定在低模量端已经触到当前固定拓扑下的物理下限；第二，高模量端的实测支撑还不够宽，仍然需要继续扩展半径扫描范围，才能把 `E_(target)` 在更宽区间内稳定反求成 `r_eff(x)`。

在这个 bandwise 反求基线之上，当前仓库已经进一步生成了第一版整结构 FE-ready 替代设计 `fjw_iter017_replacement_design_variable_radius.npz`。其核心规则不是再去使用单纯的占据二值或 fill fraction，而是先把 variable-radius 骨架细体素上的局部半径通过稳定 `r -> E_(eff)` 支撑曲线转成表观模量，再在 coarse 设计网格上聚合成 `proxy_modulus = fill_fraction times mean(E_(eff)(r))`，最后通过上游材料插值的反函数得到 `design_cage_modulus_weighted`。因此，当前真正进入后续三工况重算的对象应是 `modulus_weighted` 代理场，而不是没有明确力学意义的 fill fraction 场。

这一步同样需要在论文里保持边界清楚。`design_cage_modulus_weighted` 只是第一版可运行代理场，它已经比直接占据回代更接近“目标模量 -> 几何参数 -> 等效模量”的正式定义，但它仍然是标量、各向同性、并且受当前窄半径支撑区间限制。学术上更强的终版结论仍然要求：一，继续扩展稳定半径扫描范围；二，在需要时引入 seed 密度或单元尺度共同标定；三，用远端三工况真实重算结果验证 `E_(eff)(x) approx E_(target)(x)` 的结构级后果。

== 6.7 实验六：可选对照验证

若后续获得可信的商业有限元或既有实验输出，可进行可选对照验证。对照内容包括输入模型、节点集和单元集、材料分配、载荷块、位移向量、骨量更新、目标函数、梯度和最终设计变化。当前仓库已经有两类可直接引用的内部证据：第一类是 `datasets/sfepy_validation/rigid2_report.json` 中的刚体约束误差，第二类是 `runs/fjw_optimize_real/validation_report.json` 中的结构性检查结果。

由于不同求解器在约束实现、线性求解容差和浮点细节上存在差异，对照实验应给出误差阈值来源。小模型应追求接近机器精度的公式一致性，完整模型则应根据网格规模、约束处理和线性求解精度设定合理容差。

#todo[待填：外部黄金对照或商业有限元结果；待填：位移误差、目标函数误差、梯度误差和误差阈值说明。]

对照实验的第一层是输入一致性。需要确认节点坐标、单元连接、材料分配、边界条件和载荷在两条求解路径中含义一致。若输入本身不同，后续位移误差没有解释价值。第二层是位移一致性。可以比较全局位移向量的最大绝对误差、相对误差、关键节点位移和最大位移位置。第三层是下游一致性。即使位移误差较小，也需要检查 `SF`、骨密度更新、终态骨量和梯度是否保持一致。

当前已经量化的一项内部误差检查来自 `rigid2` 刚体约束验证：`max_top_error = 3.3881317890172014e-21`，`mean_top_error = 2.710505431213761e-22`，`solved_rotation_norm = 5.6399959177557035e-05`，并且 `verification_passed = true`。这说明参考点刚体耦合的一个独立基准算例已经达到数值上可忽略的误差量级。

当前 validation 汇总还给出另一类事实：在没有历史 MATLAB + Abaqus golden 的情况下，`historical_equivalence_claim` 只能写成 `cannot_prove_historical_equivalence_without_matching_golden_outputs`。因此，本节当前阶段能成立的结论是“内部结构检查与独立刚体约束验证已经通过或给出可解释结果”，还不能写成“已经完成外部数值等价证明”。

对照结果应允许合理差异。不同线性求解器的容差、约束消元方式和浮点顺序都可能造成微小差别。论文应关注这些差别是否影响优化判断，例如梯度主要高值区域是否一致、设计更新方向是否相近、终态骨量趋势是否一致。若误差集中在局部区域，需要结合网格、边界和材料分布解释原因。

#figure(
  image("../../Post process/figure/65/output/6-5-validation-and-error-summary.png", width: 100%),
  caption: [对照误差汇总图。当前汇总 rigid2 刚体约束误差、validation 状态分布以及现有失败来源。],
)

== 6.8 结果汇总模板

#set par(first-line-indent: 0em)
#table(
  columns: (3.0cm, 3.0cm, 6.8cm),
  align: (center, center, left),
  [实验], [当前状态], [后续需要填入的证据],
  [输入完整性], [已完成装配检查], [节点、单元、材料组、载荷表、约束集合],
  [小模型公式], [已完成公式检查], [非零公式导数、有限差分误差、最小链路方向检查],
  [三工况闭环], [已完成真实 checkpoint 整理], [目标函数、约束、设计变化、三工况骨量、validation 说明],
  [完整模型求解], [已部分完成], [运行时间、位移云图、收敛状态、内存、矩阵规模],
  [可选对照], [已具备内部误差基线], [rigid2 误差、外部黄金对照、位移误差、目标误差、梯度误差],
)
#set par(first-line-indent: 2em)

这张结果汇总表在论文后期应升级为正式实验总览。每一行都应给出运行目录、主要参数、完成状态、核心指标和结论强度。完成状态建议分为“已完成并通过验证”“已完成但存在警告”“部分完成”“待条件满足”。结论强度建议分为“公式验证”“链路验证”“性能验证”“结果验证”。这样读者可以清楚区分方法已经证明到哪一步，哪些结论仍依赖后续实验。

= 第 7 章 总结与展望

== 7.1 本文工作总结

本文围绕椎间融合器骨改建拓扑优化问题，建立了从有限元平衡、力学刺激因子、骨密度递推、伴随灵敏度到 MMA 设计更新的完整数学框架。本文重点回答三个问题：设计变量如何通过材料刚度影响位移场，位移场如何影响骨密度演化，终态骨量如何通过伴随方程反向影响设计更新。

本文当前已经完成五项核心工作。第一，给出了融合器-骨组织耦合系统的变量、区域、材料插值、目标函数和约束定义。第二，推导了多时间步骨改建模型中的伴随载荷、伴随位移方程、骨密度伴随递推和设计变量梯度。第三，把三工况优化的数据流、验证层级和结果图组织成统一的论文证据链。第四，基于 `runs/fjw_optimize_real/iter_017` 整理出一轮真实完整参考模型闭环运行结果，包括三工况骨量、目标函数、约束、设计变化、位移云图、对照误差汇总图以及求解时间、自由度规模、矩阵非零元数量和峰值内存。第五，围绕可制造 Voronoi 骨架补出第一版真实标定层，包括稳定的 `r -> E_(eff)` 局部 FE 实测曲线、bandwise 反求 lookup，以及第一版 `modulus_weighted` 整结构回代代理场。

因此，本文当前阶段已经不只是“方法写清楚”，还完成了“一轮真实闭环跑通并可复查”的结果整理，并把制造化链路中的标定层从概念补到可执行基线。现阶段最强的结论是：完整参考模型上的一轮真实三工况 forward / adjoint / MMA 链路已经成立，`iter_017` 这一轮的数据证据链完整，而且 Voronoi 骨架的局部半径标定路径已经具备真实 FE 支撑；更强的收敛性、结构级等效性、外部等价性和最终结构优越性结论仍需后续多轮实验与远端三工况回代结果支持。

== 7.2 创新点与贡献

本文的贡献可概括为四点。

#set par(first-line-indent: 0em)
1. 将椎间融合器设计表述为多材料有限元、骨密度动态递推和体积约束优化的耦合问题，明确了目标骨量最大化的数学结构。\
2. 针对多时间步骨改建目标推导伴随灵敏度，使终态骨量对大规模设计变量的梯度可以通过反向递推计算。\
3. 明确提出并初步实现“伪密度目标模量场 -> Voronoi 几何参数场 -> 局部等效模量”的标定层，给出以杆径为主变量的第一版局部 FE 标定曲线、bandwise lookup 和 `modulus_weighted` 整结构代理回代规则。\
4. 建立三工况优化与审计框架，明确每轮迭代需要保存的正向、反向、聚合和优化证据，并把真实 `iter_017` checkpoint、统一后处理图表和论文图号一一对齐，使每张结果图都能由代码和运行产物直接回放生成。
#set par(first-line-indent: 2em)

这四点贡献之间存在递进关系。第一点解决“问题如何定义”，第二点解决“梯度如何计算”，第三点解决“制造化后的几何如何重新连回力学目标”，第四点解决“结果如何证明并如何复现”。若缺少问题定义，优化结果缺乏物理目标；若缺少伴随推导，大规模设计变量无法有效更新；若缺少标定层，Voronoi 几何只能解释为空间分布趋势，不能解释为目标刚度的等效表达；若缺少验证框架和统一图表产线，实验结果难以被复查。本文当前版本围绕这条递进关系组织章节，并已经用第 6 章的真实图表和 `Post process/figure/` 下的脚本把第四点落到实处。

== 7.3 不足与后续工作

当前论文仍处于“真实一轮闭环已完成，标定层已有第一版基线，系统性结论尚未完成”的阶段，主要不足包括：完整模型仍缺少多轮优化收敛证据，Voronoi 标定层的高模量端支撑还不够宽，真实三工况 `modulus_weighted` 回代 compare 结果尚未返回，三工况载荷的最终物理解释表尚未补齐，骨改建参数来源需要进一步文献化，外部黄金对照尚未建立，目标函数和约束还可扩展到应力、孔隙连通性和制造约束。与 `iter_017` 直接对应的补充图表目前已经进一步扩展到最终体素骨架三视角、目标骨区刺激与密度增量分布、单轮分阶段耗时拆解，因此“单轮证据链”这一层已经比正文主图更完整。

后续工作按四条线推进。第一，继续扩展局部 Voronoi 半径扫描，补宽稳定 `r -> E_(eff)` 支撑区间，并视需要把 seed 密度或单元尺度并入联合标定。第二，在当前 `modulus_weighted` 代理场基础上完成远端三工况真实回代 compare，补齐位移、`bone_s`、骨密度增量和目标骨量的结构级等效证据。第三，继续完成完整参考模型的多轮真实优化，补齐目标函数收敛曲线、设计变化轨迹和最终结构结论。第四，在现有一轮真实性能画像基础上，进一步系统记录单工况和三工况的残差曲线、线性求解迭代次数和分阶段耗时，并在条件允许时进行可选对照实验，量化不同求解路径在位移、骨量、目标函数和梯度上的误差。

后续还需要补充制造约束。拓扑优化得到的密度场通常需要经过阈值化、平滑或几何重建才能成为可制造结构。融合器植入物还需要满足最小孔径、最小支柱直径、连通孔隙和表面质量等要求。本文当前目标函数只关注终态骨量和材料用量，尚未把制造约束写入优化模型。后续可以加入密度滤波、投影函数或显式几何约束，使优化结果更接近实际植入物设计。

另一个后续方向是参数敏感性分析。骨改建阈值、时间步长、材料模量、载荷大小和目标区域选择都会影响最终结果。正式论文中应至少选择若干关键参数进行扰动，观察终态骨量、优化结构和梯度分布的变化。若某个结论只在极窄参数范围内成立，需要在讨论中降低结论强度；若多个参数扰动下结构趋势一致，则说明模型具有更好的稳健性。

最终论文定稿时，结论部分应从“真实一轮闭环已证明”继续推进到“多轮优化趋势和外部对照已证明”。当前版本已经完成统一后处理目录、图号映射、图像生成脚本和一批真实结果图，后续需要继续把剩余待填项替换为具体数值，并在第 7 章明确回答：优化后终态骨量是否持续提高、材料约束是否稳定满足、三工况表现是否均衡、伴随梯度是否通过更强验证、完整模型求解成本是否可接受。

= 参考文献

#set par(first-line-indent: 0em)

[1] Wolff J. The Law of Bone Remodeling. Berlin: Springer, 1986.

[2] Huiskes R, Ruimerman R, van Lenthe G H, et al. Effects of mechanical forces on maintenance and adaptation of form in trabecular bone. Nature, 2000.

[3] Bendsøe M P, Sigmund O. Topology Optimization: Theory, Methods, and Applications. Berlin: Springer, 2003.

[4] Svanberg K. The method of moving asymptotes: a new method for structural optimization. International Journal for Numerical Methods in Engineering, 1987.
[5] Kang H, Hollister S J, La Marca F, et al. Porous biodegradable lumbar interbody fusion cage design and fabrication using integrated global-local topology optimization with laser sintering. Journal of Biomechanical Engineering, 2013.

[6] Ho N C, et al. Evaluation of Topology Optimization Using 3D Printing for Bioresorbable Fusion Cages: A Biomechanical Study in a Porcine Model. Spine, 2023.

[7] A lattice topology optimization of cervical interbody fusion cage and finite element comparison with ZK60 and Ti-6Al-4V cages. 2021.

[8] Robling A G, Turner C H. Mechanical signaling for bone modeling and remodeling. Critical Reviews in Eukaryotic Gene Expression, 2009.

[9] Martínez-Reina J, García-Aznar J M, Domínguez J, et al. Connecting mechanics and bone cell activities in the bone remodeling process: an integrated finite element modeling. Journal of the Mechanical Behavior of Biomedical Materials, 2014.

[10] Giorgio I, dell'Isola F, Andreaus U, et al. On mechanically driven biological stimulus for bone remodeling as a diffusive phenomenon. Biomechanics and Modeling in Mechanobiology, 2019.

[11] Bendsøe M P, Kikuchi N. Generating optimal topologies in structural design using a homogenization method. Computer Methods in Applied Mechanics and Engineering, 1988.

[12] Bendsøe M P, Sigmund O. Material interpolation schemes in topology optimization. Archive of Applied Mechanics, 1999.

[13] Tang T, Wang L, Zhu M, et al. Topology Optimization: A Review for Structural Designs Under Statics Problems. Materials, 2024.

[14] Kazakis G, Kanellopoulos I, Sotiropoulos S, et al. Topology optimization aided structural design: Interpretation, computational aspects and 3D printing. Heliyon, 2017.

[15] Deaton J D, Grandhi R V. A survey of structural and multidisciplinary continuum topology optimization: post 2000. Structural and Multidisciplinary Optimization, 2014.

#todo[参考文献仍需后续按学校格式统一排版，并补齐页码、卷期、DOI 与中文文献规范信息。]

= 附录 A 符号表

#table(
  columns: (2.4cm, 10.2cm),
  align: (center, left),
  [`x_i`], [第 `i` 个融合器设计单元的相对材料密度],
  [`b_j^t`], [第 `j` 个目标骨单元在时间步 `t` 的骨密度],
  [`u^t`], [时间步 `t` 的有限元位移向量],
  [`K(x,b^t)`], [由当前设计和当前骨密度装配得到的全局刚度矩阵],
  [`SF_j^t`], [第 `j` 个目标骨单元的力学刺激因子],
  [`R(SF)`], [骨改建速率函数],
  [`psi^t`], [骨密度伴随变量],
  [`lambda^t`], [伴随位移变量],
  [`F_v^t`], [动态伴随载荷],
  [`J(x)`], [最小化形式下的总体目标函数],
  [`g(x)`], [材料用量约束],
)

= 附录 B 图表状态清单

已在当前稿中绘制的流程图包括：图 1-1 研究主线、图 1-2 技术路线、图 2-1 有限元正向输入输出、图 3-1 正向/反向时间步关系、图 4-1 算法闭环、图 5-1 三工况迭代数据流、图 5-2 验证层级。

第 1 章当前使用的结果图已经全部纳入 `Post process/figure/` 的统一后处理入口。图 1-1 到图 1-12 现在都具备独立 `render.py`、独立 `README.md` 和对应 `output/` 目录，且生成时优先使用 `iter_017` 相关真实产物；`docs/chapter1/` 下的旧脚本仅保留作历史参考。

当前已经完成真实数据生成并接入正文或统一后处理目录的结果图包括：图 6-1 完整参考模型区域分布图、图 6-2 小模型正向位移与伴随载荷示意、图 6-3 三工况优化指标变化、图 6-4 完整模型位移云图、图 6-5 对照误差汇总图，以及补充图 6-6 `iter_017` 最终晶格体素骨架三视角、补充图 6-7 目标骨区刺激与密度增量分布、补充图 6-8 `iter_017` 分阶段耗时拆解、补充图 6-9 当前可用真实迭代趋势摘要、补充图 6-10 `iter_017` 目标骨区前后骨密度空间变化、补充图 6-11 `iter_017` 设计灵敏度空间分布、补充图 6-12 `iter_017` 设计变量更新量空间分布、补充图 6-13 `iter_017` 当前可用骨密度时间步摘要、补充图 6-14 `iter_017` 最终优化结构高保真三维展示。

当前仍待补充的图表主要包括：更连续的多轮优化收敛曲线、更长时间跨度的骨密度时序变化图、外部黄金对照误差图以及参数敏感性分析图。

= 附录 C 待填数据清单

1. 作者、学号、学院、导师和提交日期。\
2. 正式文献综述和参考文献格式。\
3. 骨改建参数表、单位和来源。\
4. 三工况载荷的方向、单位、数值和物理解释。\
5. 完整模型装配时间、峰值内存和机器配置。\
6. 小模型正向、伴随和有限差分梯度验证表。\
7. 三工况闭环的目标函数、约束、设计变化和验证报告摘要。\
8. 完整参考模型三工况求解时间、峰值内存、最大位移和收敛状态。\
9. 可选对照误差表。\
10. 多轮优化最终结构、骨密度时序变化图、收敛曲线和结论性判断。
